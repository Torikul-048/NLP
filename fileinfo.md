# 📑 KUET Academic IR — ফাইলের বিস্তারিত নির্দেশিকা ও ফাংশন ডায়েরি (`fileinfo.md`)

এই নথিতে আমাদের **KUET CSE সিলেবাস ভিত্তিক একাডেমিক ইনফরমেশন রিট্রিভাল (Academic IR)** সিস্টেমের প্রতিটি ফাইলের উদ্দেশ্য, স্থাপত্যগত ভূমিকা এবং ফাইলের মধ্যকার প্রতিটি ক্লাস ও ফাংশনের ইনপুট-আউটপুট ও অভ্যন্তরীণ কার্যপ্রণালী বিস্তারিতভাবে বাংলা ভাষায় লিপিবদ্ধ করা হলো।

---

## ১. প্রকল্পের সার্বিক স্থাপত্য ও ফাইল ওভারভিউ (High-Level File Inventory & Purpose)

আমাদের সম্পূর্ণ প্রজেক্টটি একটি মডুলার, এক্সটেনসিবল এবং প্রোডাকশন-গ্রেড আর্কিটেকচারে গঠিত। নিচে সিস্টেমের প্রতিটি ফাইলের নাম, তার ক্যাটাগরি এবং প্রকল্পের সামগ্রিক পাইপলাইনে তার মূল দায়িত্ব তুলে ধরা হলো:

| ক্রম | ফাইলের পাথ (File Path) | ক্যাটাগরি (Category) | মূল উদ্দেশ্য ও প্রকল্পগত ভূমিকা (Core Purpose & Role) |
| :---: | :--- | :--- | :--- |
| ১ | `src/utils.py` | Core Utility | ফাইল পাথ রেজোলিউশন, ক্যাশ এনভায়রনমেন্ট সেটআপ, নিরাপদ JSON I/O, ক্রিপ্টোগ্রাফিক কর্পাস হ্যাশিং এবং $L_2$ রো ভেক্টর নরমালাইজেশন। |
| ২ | `src/preprocessing.py` | NLP Processing | রেগুলার এক্সপ্রেশনের মাধ্যমে কোর্স কোড স্ট্যান্ডার্ডাইজেশন, টেকনিক্যাল টোকেন প্রিজার্ভেশন, লেম্যাটাইজেশন এবং লেভেনশটাইন এডিট ডিসট্যান্স ভিত্তিক টাইপো সংশোধন। |
| ৩ | `src/query_parser.py` | Query Understanding | ব্যবহারকারীর প্রশ্ন থেকে ১১টি ভিন্ন ইনটেন্ট শনাক্তকরণ, কোর্স কোড/টাইটেল ও ইয়ার/টার্ম ফিল্টারিং এবং নয়েজ শব্দ অপসারণ করে সার্চ কুয়েরি তৈরি। |
| ৪ | `src/answer_formatter.py` | Natural Language Gen | রিট্রিভ করা ডেটা থেকে মানুষের পাঠযোগ্য নির্ভুল উত্তর তৈরি, ইনটেন্ট অনুযায়ী ফরম্যাটিং, ডোমেইন ডিসক্লেইমার প্রদান এবং ফলস প্রেমিস সনাক্তকরণ। |
| ৫ | `src/registry.py` | Factory / Lifecycle | চারটি মডেল (Model A, B, C, D)-এর ডায়নামিক ইনিশিয়ালাইজেশন, ওয়ার্ড২ভেক ফাইলের পাথ রেজোলিউশন এবং আর্টফ্যাক্ট লোডিং হ্যান্ডলার। |
| ৬ | `src/evaluation.py` | Metric & Benchmark | রেঙ্কিং মেট্রিক্স (Hit@1, Hit@3, MRR, Recall@3), ব্যালেন্সড এক্যুরেসি ভিত্তিক থ্রেশহোল্ড ক্যালিব্রেশন এবং রিজেকশন/ওওডি পারফরম্যান্স মূল্যায়ন। |
| ৭ | `src/datasets.py` | Dataset Management | CSV কুয়েরি রিডার, গোল্ড লেবেল প্রসেসর, ট্রেইন-ভ্যালিডেশন-টেস্ট স্প্লিট লিকেজ অডিটর এবং হার্ড ও র‍্যান্ডম নেগেটিভ স্যাম্পলিং ইঞ্জিন। |
| ৮ | `src/corpus/pdf_extractor.py` | Raw Data Extraction | PyMuPDF লাইব্রেরি ব্যবহার করে মূল সিলেবাস PDF ফাইলের প্রতিটি পৃষ্ঠা থেকে টেক্সট ও পেজ নম্বর নির্ভুলভাবে এক্সট্র্যাক্ট করা। |
| ৯ | `src/corpus/corpus_builder.py` | Corpus Pipeline | রেজেক্স দিয়ে কোর্স হেডিং, ক্রেডিট, কন্টাক্ট আওয়ার, সিলেবাস টেক্সট পার্সিং, সামারি টেবিল অডিট এবং বৈপরীত্য (conflicts) ডিটেকশন পাইপলাইন। |
| ১০ | `src/models/base.py` | Abstract Base Class | সকল রিট্রিভাল মডেলের জন্য কমন প্যারেন্ট ক্লাস (`BaseRetriever`), যা টু-স্টেজ পাইপলাইন, ম্যানিফেস্ট অডিট এবং রেজাল্ট ফিল্টারিং নিশ্চিত করে। |
| ১১ | `src/models/word_vectors.py` | Word Embeddings | প্রি-ট্রেইন্ড Google News Word2Vec লোডার, কেস-ইনসেনসিটিভ কি-লুকআপ এবং OOV টোকেন দূর করে কমপ্যাক্ট সিকোয়েন্স ভেক্টর নির্মাতা। |
| ১২ | `src/models/model_a_tfidf.py` | Sparse Retrieval | মডেল এ: Scikit-Learn ভিত্তিক ইউনিগ্রাম+বাইগ্রাম TF-IDF ভেক্টরাইজার এবং কসাইন সিমিলারিটি ভিত্তিক স্পার্স রিট্রিভার। |
| ১৩ | `src/models/model_b_word2vec.py` | Dense Semantic | মডেল বি: TF-IDF ওয়েটেড Word2Vec সেন্ট্রয়েড ভেক্টর এমবেডার এবং আনসিন টার্মের জন্য IDF স্মুথিং সম্বলিত রিট্রিভার। |
| ১৪ | `src/models/model_c_bilstm.py` | Deep Neural Retrieval | মডেল সি: PyTorch ভিত্তিক দ্বি-মুখী LSTM (BiLSTM) এনকোডার ও MLP ক্লাসিফায়ার সম্বলিত এন্ড-টু-এন্ড রেলিভেন্স ম্যাচিং নিউরাল নেটওয়ার্ক। |
| ১৫ | `src/models/model_d_sbert.py` | Transformer Dense | মডেল ডি: প্রি-ট্রেইন্ড Sentence-BERT (`all-MiniLM-L6-v2`) আর্কিটেকচার ভিত্তিক ৩৮৪-মাত্রার ডেন্স ভেক্টর রিট্রিভার। |
| ১৬ | `scripts/build_corpus.py` | Pipeline Automation | সিলেবাসের কাঁচা PDF ফাইল থেকে স্বয়ংক্রিয়ভাবে `courses.json`, `courses.csv` এবং `corpus_statistics.json` তৈরির CLI স্ক্রিপ্ট। |
| ১৭ | `scripts/prepare_assets.py` | Setup & Download | Word2Vec SLIM মডেল (৩০০k ওয়ার্ড) ও NLTK WordNet ডাটাসেট স্বয়ংক্রিয়ভাবে ডাউনলোড ও লোকাল ক্যাশে সংরক্ষণের স্ক্রিপ্ট। |
| ১৮ | `scripts/build_all_indexes.py` | Indexing Engine | সবকটি মডেলের (Model A, B, C, D) ইনডেক্সিং প্রি-কম্পিউট করে `artifacts/` ডিরেক্টরিতে সেভ করার কমান্ড-লাইন ইউটিলিটি। |
| ১৯ | `scripts/train_model_c.py` | Neural Training | মডেল সি-এর BiLSTM নেটওয়ার্কের ফুল ট্রেনিং লুপ, Adam অপটিমাইজার, পজিটিভ ওয়েটেড লস এবং আর্লি স্টপিং ভিত্তিক ট্রেইনিং স্ক্রিপ্ট। |
| ২০ | `scripts/calibrate_thresholds.py` | Threshold Tuning | ভ্যালিডেশন কুয়েরিগুলোর ওপর গ্রিড সার্চ চালিয়ে প্রতিটি মডেলের জন্য অপটিমাল ডিসিশন থ্রেশহোল্ড নির্ধারণ ও সংরক্ষণকারী স্ক্রিপ্ট। |
| ২১ | `scripts/evaluate_all.py` | Evaluation Suite | টেস্ট সেটের ৪৮টি আনসিন কুয়েরির ওপর সব মডেলের ইভ্যালুয়েশন চালিয়ে তুলনামূলক CSV রিপোর্ট, রান ম্যানিফেস্ট ও অটোমেটিক প্লট তৈরির স্ক্রিপ্ট। |
| ২২ | `src/plotting.py` | Metric & Visualization | কনফিউশন ম্যাট্রিক্স (২x২), এক্যুরেসি, প্রেসিশন, রিকল, F1, স্পেসিফিসিটি, স্কোর ডিস্ট্রিবিউশন ও ক্রস-মডেল তুলনামূলক হাই-রেজোলিউশন (৩০০ DPI) প্লটিং ইঞ্জিন। |
| ২৩ | `scripts/generate_plots.py` | Plotting Automation | কমান্ড লাইন থেকে এক ক্লিকে `plotting/` ফোল্ডার এবং মডেলভিত্তিক সাবফোল্ডারে সমস্ত রেজাল্ট প্লট জেনারেট করার CLI স্ক্রিপ্ট। |
| ২৪ | `tests/test_core.py` | Core Unit Testing | কর্পাস পার্সিং, হেডিং কন্টিনিউয়েশন, টেকনিক্যাল টার্মস, পার্সার ইনটেন্ট, লিকেজ অডিট এবং স্টেল ম্যানিফেস্ট টেস্টকারী পাইটেস্ট ফাইল। |
| ২৫ | `tests/test_neural.py` | Neural Unit Testing | BiLSTM-এর প্যাকড প্যাডিং ইনভেরিয়েন্স, আউট-অব-কর্পাস ওয়ার্ড প্রিজার্ভেশন এবং টয়-পেয়ার লার্নিং ভ্যালিডেশন টেস্ট। |
| ২৬ | `tests/test_sbert.py` | Transformer Testing | Sentence-BERT ইনডেক্সের ১০৮টি কোর্সের ৩৮৪-মাত্রার ডাইমেনশন এবং ইউনিট নর্ম ($L_2 = 1.0$) নিশ্চিতকারী টেস্ট। |
| ২৭ | `tests/test_app.py` | UI Integration Test | Streamlit অ্যাপের ব্যাকগ্রাউন্ড হেডলেস টেস্টিং, মডেল সিলেকশন ড্রপডাউন পরিবর্তন এবং লাইভ রেসপন্স যাচাইকরণ টেস্ট। |
| ২৮ | `app.py` | Interactive Web UI | স্ট্রিমলিট ভিত্তিক আকর্ষণীয় ওয়েব ইউজার ইন্টারফেস, রিয়েল-টাইম কুয়েরি ইঞ্জিন, ইন্টারঅ্যাক্টিভ ইভ্যালুয়েশন ড্যাশবোর্ড ও রেজাল্ট চার্ট ডিসপ্লে। |
| ২৯ | `start_app.bat` | Deployment Automation | উইন্ডোজে এক ক্লিকে স্বয়ংক্রিয়ভাবে `plotting/` ফোল্ডারে মডেলভিত্তিক সমস্ত ফলাফল প্লট জেনারেট করে Streamlit অ্যাপ চালু করার ব্যাচ স্ক্রিপ্ট। |
| ৩০ | `requirements.txt` | Dependency Config | প্রকল্পের জন্য প্রয়োজনীয় পাইথন প্যাকেজের ভার্সন রেঞ্জ নির্ধারণকারী ফাইল (Matplotlib অন্তর্ভুক্ত)। |
| ২৯ | `requirements-tested.txt` | Exact Dependency Freeze | ডেভেলপমেন্ট ও টেস্টিংয়ের সময় ব্যবহৃত শতভাগ পরীক্ষিত ও লকড লাইব্রেরি ভার্সনের তালিকা। |
| ৩০ | `README.md` | Documentation | সম্পূর্ণ প্রকল্পের ইনস্টলেশন, ইনডেক্সিং, ট্রেইনিং এবং রান করার প্রাথমিক গাইড ও নির্দেশিকা। |
| ৩১ | `IMPLEMENTATION_REPORT.md` | Technical Report | আর্কিটেকচারাল ডিজাইন, ম্যাট্রিক্স ডাইমেনশন, স্যাম্পলিং স্ট্র্যাটেজি এবং রিট্রিভাল ট্রেড-অফের টেকনিক্যাল রিপোর্ট। |
| ৩২ | `explain.md` | Deep Technical Viva Guide | ভাইভা এবং ডিপ টেকনিক্যাল বোঝাপড়ার জন্য ২১টি বিস্তৃত অধ্যায়ে রচিত পূর্ণাঙ্গ বাংলা সহায়িকা। |
| ৩৩ | `report/main.tex` & `main.pdf` | Academic Project Report | একাডেমিক সাবমিশনের জন্য প্রস্তুতকৃত সম্পূর্ণ প্রফেশনাল LaTeX প্রজেক্ট রিপোর্ট ও তার কম্পাইল করা PDF। |
| ৩৪ | `data/raw/CourseContentsCSE_14_08_2022.pdf` | Raw Curriculum Document | কুয়েট সিএসই বিভাগের অফিসিয়াল ১১২ পৃষ্ঠার সিলেবাস ডকুমেন্ট (অরিজিনাল সোর্স)। |
| ৩৫ | `data/processed/courses.json` | Parsed Course Database | সিলেবাস থেকে নিষ্কাশিত ১০৮টি কোর্সের পূর্ণাঙ্গ মেটাডেটা, সিলেবাস টেক্সট ও সার্চ টেক্সট সম্বলিত প্রাথমিক ডেটাবেজ। |
| ৩৬ | `data/processed/courses.csv` | Tabular Course Database | গবেষকদের জন্য সহজে স্প্রেডশিটে দেখার উপযোগী কোর্স ডেটাসেটের কমা-সেপারেটেড সংস্করণ। |
| ৩৭ | `data/processed/corpus_statistics.json` | Corpus Health Audit | ইউনিক কোর্স কোড, থিওরি/ল্যাব বিন্যাস, মিসিং ফিল্ড এবং সোর্স বৈপরীত্যের পরিসংখ্যান রিপোর্ট। |
| ৩৮ | `data/queries/aliases.json` | Acronym Dictionary | সংক্ষিপ্ত টেকনিক্যাল রূপ (যেমন NLP, ML, OS, DBMS) থেকে পূর্ণাঙ্গ রূপে রূপান্তরের ম্যাপিং ফাইল। |
| ৩৯ | `data/queries/relevance_queries.csv` | Training/Val Benchmark | মডেল ট্রেনিং এবং থ্রেশহোল্ড ক্যালিব্রেশনের জন্য ব্যবহৃত ৭২টি লেবেলযুক্ত কুয়েরির ডেটাসেট। |
| ৪০ | `data/queries/evaluation_queries.csv` | Independent Test Benchmark | মডেলগুলোর নিরপেক্ষ মূল্যায়নের জন্য সংরক্ষিত ৪৮টি আনসিন কুয়েরির গোল্ড টেস্ট ডেটাসেট। |

---

---

## ২. কোর সোর্স কোড (`src/`) — ফাইল ও ফাংশনভিত্তিক গভীর ব্যবচ্ছেদ

প্রকল্পের মূল ব্যবসায়িক লজিক, টেক্সট প্রসেসিং, ইনটেন্ট পার্সিং, অ্যান্সার ফরম্যাটিং এবং ইভ্যালুয়েশন পাইপলাইন রয়েছে `src/` ফোল্ডারের অধীনে। নিচে প্রতিটি ফাইলের ফাংশনগুলোর বিস্তারিত বিবরণ দেওয়া হলো:

### ২.১. `src/utils.py` (সার্বজনীন ইউটিলিটি ও ফাইল প্রসেসিং)

এই ফাইলটি পুরো প্রজেক্টের পাথ রেজোলিউশন, ক্যাশিং এনভায়রনমেন্ট ভ্যারিয়েবল সেটআপ, সেফ JSON ডেটা আদান-প্রদান এবং ভেক্টর গণনার সাধারণ গাণিতিক অপারেশন সম্পাদন করে।

#### গ্লোবাল এনভায়রনমেন্ট ও কনস্ট্যান্ট:
* `ROOT`: প্রজেক্টের রুট ডিরেক্টরির পাথ (`Path(__file__).resolve().parents[1]`)।
* `HF_HOME`, `GENSIM_DATA_DIR`, `NLTK_DATA`: ক্যাশ ডিরেক্টরি সেট করে দেওয়া হয়েছে যাতে যেকোনো এক্সটার্নাল লাইব্রেরি লোকাল প্রজেক্ট ডিরেক্টরির ভেতরেই মডেল ও ডেটা সংরক্ষণ করে।

#### ফাংশনসমূহ:

##### ১. `read_json(path)`
* **সিগনেচার:** `read_json(path: str | Path) -> dict | list`
* **প্যারামিটার:** `path` — যে JSON ফাইলটি পড়তে হবে তার ফাইল পাথ।
* **রিটার্ন:** পার্স করা পাইথন ডিকশনারি অথবা লিস্ট।
* **কার্যপ্রণালী:** পাথ অবজেক্টে রূপান্তর করে `utf-8` এনকোডিং নিশ্চিত করে ফাইল ওপেন করে এবং `json.loads` এর মাধ্যমে ডেটা রিটার্ন করে। এটি ইউনিকোড অক্ষর (যেমন বাংলা বা বিশেষ গাণিতিক প্রতীক) নিরাপদে লোড করে।

##### ২. `write_json(path, value)`
* **সিগনেচার:** `write_json(path: str | Path, value: Any) -> None`
* **প্যারামিটার:** `path` — টার্গেট ফাইল পাথ; `value` — যে পাইথন অবজেক্ট (ডিকশনারি/লিস্ট) সংরক্ষণ করতে হবে।
* **রিটার্ন:** `None`।
* **কার্যপ্রণালী:** 
  1. ফাইলের প্যারেন্ট ডিরেক্টরি বিদ্যমান না থাকলে `path.parent.mkdir(parents=True, exist_ok=True)` দিয়ে ডিরেক্টরি তৈরি করে।
  2. `json.dumps(value, indent=2, ensure_ascii=False)` ব্যবহার করে সুন্দরভাবে ইনডেন্টেশন দিয়ে ফাইল লেখে। `ensure_ascii=False` ব্যবহারের ফলে ইউনিকোড ক্যারেক্টার বিকৃত হয় না।

##### ৩. `load_courses()`
* **সিগনেচার:** `load_courses() -> list[dict]`
* **প্যারামিটার:** কোনো প্যারামিটার নেই।
* **রিটার্ন:** ১০৮টি কোর্সের ডিকশনারি সম্বলিত লিস্ট।
* **কার্যপ্রণালী:** `data/processed/courses.json` পাথ থেকে প্রসেস করা সিলেবাস ডেটাবেজ লোড করে। যদি ফাইলটি পাওয়া না যায়, তবে ব্যবহারকারীকে সুস্পষ্ট `FileNotFoundError` দেখায় এবং প্রথমে `python -m scripts.build_corpus` রান করতে নির্দেশ দেয়।

##### ৪. `corpus_hash(courses)`
* **সিগনেচার:** `corpus_hash(courses: list[dict]) -> str`
* **প্যারামিটার:** `courses` — ১০৮টি কোর্সের অবজেক্ট লিস্ট।
* **রিটার্ন:** ৬৪ ক্যারেক্টারের SHA-256 হেক্সাডেসিমেল হ্যাশ স্ট্রিং।
* **কার্যপ্রণালী:** ডিকশনারিটির কি-গুলোকে ক্যানোনিকালি সর্ট করে (`sort_keys=True`) JSON স্ট্রিং তৈরি করে এবং তার SHA-256 হ্যাশ গণনা করে। এটি প্রজেক্টের **কর্পাস ফিঙ্গারপ্রিন্ট** হিসেবে কাজ করে। কোনো মডেল ইনডেক্স তৈরি করার সময় এই হ্যাশ সেভ রাখা হয়; পরবর্তীতে সিলেবাসে কোনো পরিবর্তন আসলে সিস্টেম স্বয়ংক্রিয়ভাবে অসঙ্গতি শনাক্ত করে রি-ইনডেক্সিং দাবি করে।

##### ৫. `normalize_rows(values)`
* **সিগনেচার:** `normalize_rows(values: np.ndarray | list) -> np.ndarray`
* **প্যারামিটার:** `values` — এক বা একাধিক ভেক্টর সম্বলিত 1D বা 2D অ্যারে।
* **রিটার্ন:** $L_2$ নরম্যালাইজড ফ্লোট৩২ NumPy অ্যারে।
* **কার্যপ্রণালী:** 
  $$\hat{\mathbf{v}} = rac{\mathbf{v}}{\max(\|\mathbf{v}\|_2, \epsilon)}, \quad \epsilon = 10^{-12}$$
  প্রতিটি রো ভেক্টরের ইউক্লিডিয়ান দৈর্ঘ্য ($L_2$ norm) দ্বারা ভাগ করে একক দৈর্ঘ্যের ভেক্টরে পরিণত করে। শূন্য ভেক্টরের ক্ষেত্রে জিরো-ডিভিশন রোধ করতে $\epsilon = 10^{-12}$ ব্যবহার করা হয়েছে।

---

### ২.২. `src/preprocessing.py` (প্রি-প্রসেসিং ও টেক্সট নরমালাইজেশন)

এই মডিউলটি টেক্সট ক্লিনিং, রেগুলার এক্সপ্রেশনের মাধ্যমে কোড প্রমিতকরণ, টোকেনাইজেশন, অফলাইন-সেফ লেম্যাটাইজেশন এবং লেভেনশটাইন এডিট ডিসট্যান্সের মাধ্যমে স্পেলিং/টাইপো সংশোধন পরিচালনা করে।

#### গ্লোবাল কনস্ট্যান্ট ও রেজেক্স:
* `CODE_RE`: `re.compile(r"\b([A-Za-z]{2,5})[\s-]*(\d{4})\b")` — কোর্স কোড শনাক্ত করার রেজেক্স (যেমন: `CSE4121`, `cse-4121`, `Math 1101`)।
* `TOKEN_RE`: `re.compile(r"[a-z]+(?:[+#]+)?|\d+(?:\.\d+)?", re.I)` — শব্দ ও টেকনিক্যাল টার্মস যেমন `c++`, `c#`, সংখ্যা আলাদা করে।
* `TECHNICAL`: হোয়াইটলিস্টেড টেকনিক্যাল সংক্ষিপ্ত রূপের সেট (`{"os", "rsa", "pos", "bfs", "dfs", "c", "c++", "c#", "sql", "nlp", "ml"}`), যা স্টপওয়ার্ড হিসেবে বাদ দেওয়া যাবে না বা ভুল লেম্যাটাইজ করা যাবে না।

#### ফাংশনসমূহ:

##### ১. `normalize_codes(text)`
* **সিগনেচার:** `normalize_codes(text: str) -> str`
* **প্যারামিটার:** `text` — কাঁচা ইনপুট স্ট্রিং।
* **রিটার্ন:** প্রমিত কোর্স কোড সম্বলিত স্ট্রিং।
* **কার্যপ্রণালী:** রেজেক্স সাবস্টিউশনের মাধ্যমে ডিপার্টমেন্ট কোডকে আপারকোর এবং মাঝে একক স্পেস দিয়ে বিন্যস্ত করে। উদাহরণ: `"cse-4121"` বা `"cse4121"` রূপান্তরিত হয়ে `"CSE 4121"` হয়। কোনো সংখ্যাক্রম পরিবর্তন করা হয় না।

##### ২. `semantic_tokens(text)`
* **সিগনেচার:** `semantic_tokens(text: str) -> list[str]`
* **প্যারামিটার:** `text` — ইনপুট টেক্সট।
* **রিটার্ন:** লোয়ারকেস করা টোকেনের তালিকা।
* **কার্যপ্রণালী:** প্রথমে `normalize_codes` রান করে, তারপর টেক্সটকে লোয়ারকেস করে `TOKEN_RE` দিয়ে ম্যাচ করা সমস্ত টোকেন এক্সট্র্যাক্ট করে। এটি যতিচিহ্ন বাদ দেয় কিন্তু `c++` বা `c#` অক্ষুণ্ণ রাখে।

##### ৩. `lemma(token)`
* **সিগনেচার:** `lemma(token: str) -> str`
* **প্যারামিটার:** `token` — একক শব্দ/টোকেন।
* **রিটার্ন:** শব্দের মূল লেম্যাটাইজড রূপ (Base form)।
* **কার্যপ্রণালী:**
  1. `@lru_cache(maxsize=20000)` ডেকোরেটর ব্যবহারের কারণে পূর্বে প্রসেস করা শব্দ মেমরি ক্যাশ থেকে তাৎক্ষণিক রিটার্ন হয়।
  2. টোকেনটি যদি `TECHNICAL` সেটে থাকে (যেমন `"os"`, `"nlp"`), তবে কোনো পরিবর্তন না করে সরাসরি ফিরিয়ে দেয় (যাতে "os" ভুলে "o" না হয়ে যায়)।
  3. NLTK-এর `WordNetLemmatizer` কল করে। যদি NLTK ডেটাবেজ ডাউনলোড করা না থাকে বা অফলাইন মোডে থাকে (`LookupError`), তবে এটি সিস্টেমকে ক্র্যাশ না করিয়ে ফলব্যাক মেমরি ডিকশনারি (`{"systems": "system", "algorithms": "algorithm", ...}`) ব্যবহার করে বহুবচন শব্দকে একবচনে নামিয়ে আনে।

##### ৪. `lexical_tokens(text, remove_stopwords=True)`
* **সিগনেচার:** `lexical_tokens(text: str, remove_stopwords: bool = True) -> list[str]`
* **প্যারামিটার:** `text` — ইনপুট স্ট্রিং; `remove_stopwords` — স্টপওয়ার্ড ফিল্টার করার ফ্ল্যাগ (ডিফল্ট `True`)।
* **রিটার্ন:** লেম্যাটাইজড ও স্টপওয়ার্ড-মুক্ত টোকেন লিস্ট।
* **কার্যপ্রণালী:** `semantic_tokens` দিয়ে টোকেন কেটে `lemma()` ফাংশন দিয়ে লেম্যাটাইজ করে। Scikit-Learn-এর `ENGLISH_STOP_WORDS` তালিকা চেক করে ইংরেজি স্টপওয়ার্ড (`is`, `the`, `and` ইত্যাদি) বাদ দেয়, কিন্তু যদি কোনো স্টপওয়ার্ড `TECHNICAL` সেটে থাকে (যেমন `c`), তবে তা বজায় রাখে।

##### ৫. `edit_distance(a, b)`
* **সিগনেচার:** `edit_distance(a: str, b: str) -> int`
* **প্যারামিটার:** দুটি শব্দ `a` এবং `b`।
* **রিটার্ন:** শব্দ দুটির মধ্যকার সর্বনিম্ন লেভেনশটাইন এডিট ডিসট্যান্স (পূরণবাচক সংখ্যা)।
* **কার্যপ্রণালী:** ক্লাসিক্যাল ডায়নামিক প্রোগ্রামিং (DP) অ্যালগরিদম ব্যবহার করে মেমরি-অপটিমাইজড দুটি রো অ্যারে দিয়ে ইনসার্শন, ডিলিশন এবং সাবস্টিটিউশন কস্ট গণনা করে। সময় জটিলতা $O(|a| 	imes |b|)$ এবং স্থান জটিলতা $O(|b|)$।

##### ৬. `correct_typos(text, vocabulary)`
* **সিগনেচার:** `correct_typos(text: str, vocabulary: set[str]) -> tuple[str, dict[str, str]]`
* **প্যারামিটার:** `text` — ইনপুট স্ট্রিং; `vocabulary` — সিলেবাসের বৈধ ইংরেজি শব্দের ডিকশনারি/সেট।
* **রিটার্ন:** `(সংশোধিত_টেক্সট, কারেকশন_ডিকশনারি)` যেমন `("neural networks", {"netwroks": "networks"})`।
* **কার্যপ্রণালী:**
  1. টেক্সটের প্রতিটি অ্যালফাবেটিক শব্দকে ম্যাচ করে।
  2. শব্দের দৈর্ঘ্য যদি ৬ অক্ষরের কম হয় অথবা শব্দটি যদি ইতিমধ্যে ভোকাবুলারিতে থাকে, তবে তাকে পরিবর্তন করে না (যাতে ছোট শব্দ বা সঠিক শব্দ অক্ষত থাকে)।
  3. `difflib.get_close_matches` ব্যবহার করে কাটঅফ $\ge 0.83$ সহ ৪টি সম্ভাব্য শব্দ খোঁজে।
  4. এরপর `edit_distance(word, candidate) == 1` শর্ত যাচাই করে নিশ্চিত করে যে মাত্র ১টি অক্ষরের ভুল ছিল কিনা।
  5. যদি ঠিক ১টি নির্ভরযোগ্য ক্যান্ডিডেট পাওয়া যায়, তবে শব্দটি সংশোধন করে এবং লগ ডিকশনারিতে এন্ট্রি যোগ করে।

---

### ২.৩. `src/query_parser.py` (কুয়েরি পার্সিং ও ইনটেন্ট ক্লাসিফিকেশন)

ব্যবহারকারীর অগোছালো প্রশ্ন বিশ্লেষণ করে তার অন্তর্নিহিত উদ্দেশ্য (Intent) এবং মেটাডেটা আলাদা করার গুরুদায়িত্ব পালন করে এই ফাইল।

#### ক্লাস ও তাদের মেথড:

#### ক্লাস: `ParsedQuery` (ডেdataclass)
ব্যবহারকারীর পার্স করা কুয়েরির স্ট্রাকচার্ড তথ্য বহন করে।
* **ফিল্ডসমূহ:**
  * `raw_query`: ব্যবহারকারীর মূল ইনপুট।
  * `intent`: শনাক্তকৃত ইনটেন্ট (যেমন `TOPIC_SEARCH`, `CREDIT_QUERY`, `LAB_QUERY`, `GIBBERISH` ইত্যাদি)।
  * `course_code`: উল্লেখিত কোর্স কোড (যেমন `CSE 4121`) অথবা `None`।
  * `course_title`: উল্লেখিত কোর্সের নাম অথবা `None`।
  * `year`: শিক্ষাবর্ষ (যেমন `4th Year`) অথবা `None`।
  * `term`: সেমিস্টার/টার্ম (যেমন `1st Term`) অথবা `None`।
  * `search_query`: নয়েজ ও অতিরিক্ত শব্দ বাদ দিয়ে তৈরি বিশুদ্ধ রিট্রিভাল কুয়েরি।
  * `corrections`: স্পেলিং সংশোধনীর লগ ডিকশনারি।
* **মেথড:**
  * `as_dict(self) -> dict`: ডেটাক্লাসের সমস্ত প্রপার্টিকে একটি স্ট্যান্ডার্ড পাইথন ডিকশনারিতে রূপান্তর করে।

#### ক্লাস: `QueryParser`
* **`__init__(self, courses)`**
  * **প্যারামিটার:** `courses` — ১০৮টি কোর্সের ডেটাবেজ।
  * **কার্যপ্রণালী:** 
    1. `data/queries/aliases.json` থেকে সংক্ষিপ্ত রূপের তালিকা লোড করে।
    2. কোর্সের সমস্ত শিরোনাম এবং বিবরণ প্রসেস করে একটি কর্পাস ভোকাবুলারি তৈরি করে।
    3. কুয়েরি স্টপওয়ার্ডের তালিকা (`QUERY_STOP`) প্রস্তুত করে, যার মধ্যে প্রশ্নবাচক শব্দ (`what`, `who`, `which`, `tell`, `course`, `syllabus`) অন্তর্ভুক্ত।
* **`parse(self, query) -> ParsedQuery`**
  * **প্যারামিটার:** `query` — ব্যবহারকারীর টাইপ করা কাঁচা প্রশ্ন স্ট্রিং।
  * **রিটার্ন:** সম্পন্ন `ParsedQuery` অবজেক্ট।
  * **কার্যপ্রণালী (১১টি ইনটেন্ট হ্যান্ডলিং লজিক):**
    1. **`EMPTY`**: খালি বা হোয়াইটস্পেস কুয়েরি হলে তাৎক্ষণিক `EMPTY` ইনটেন্ট সেট করে।
    2. **`GIBBERISH`**: স্বরবর্ণহীন শব্দ (`asdfghjk`) বা সম্পূর্ণ অর্থহীন স্ট্রিং শনাক্ত করে।
    3. **`OUT_OF_SCOPE`**: সিলেবাসের বাইরের বিষয় (যেমন আবহাওয়া, ফুটবল, রাজনীতি, সিনেমা) রেজেক্স দিয়ে শনাক্ত করে।
    4. **`UNSUPPORTED_FIELD`**: সিলেবাসে নেই এমন তথ্য যেমন শিক্ষকের নাম, পরীক্ষার রুটিন বা ফি সংক্রান্ত প্রশ্ন শনাক্ত করে।
    5. **অ্যালিয়াস প্রতিস্থাপন ও কোড নরম্যালাইজেশন:** `NLP` থাকলে তা `Natural Language Processing` দ্বারা প্রতিস্থাপন করে এবং `cse4121` কে `CSE 4121` বানায়।
    6. **ইয়ার ও টার্ম এক্সট্র্যাকশন:** রেজেক্সের সাহায্যে "4th year", "2nd term" ইত্যাদি ফিল্টার করে `year` ও `term` ভ্যারিয়েবলে সংরক্ষণ করে।
    7. **ইনটেন্ট ডিটারমিনেশন:**
       * "credit", "how many credits" থাকলে $\to$ `CREDIT_QUERY`
       * "contact hour", "hours per week" থাকলে $\to$ `CONTACT_HOUR_QUERY`
       * "lab", "laboratory", "sessional" থাকলে $\to$ `LAB_QUERY`
       * সরাসরি কোর্স কোড থাকলে $\to$ `COURSE_CODE_QUERY`
       * কোর্স টাইটেল বা বর্ণনা সংক্রান্ত কুয়েরি হলে $\to$ `TOPIC_SEARCH`
    8. **সার্চ কুয়েরি তৈরি:** প্রশ্নবাচক শব্দ বাদ দিয়ে রিট্রিভাল মডেলের জন্য অপটিমাইজড `search_query` স্ট্রিং তৈরি করে।

---

### ২.৪. `src/answer_formatter.py` (ন্যাচারাল ল্যাঙ্গুয়েজ অ্যান্সার ফরম্যাটিং)

রিট্রিভ করা কাঁচা ডেটাকে সুন্দর, প্রাঞ্জল এবং মানুষের পাঠযোগ্য একাডেমিক উত্তরে রূপান্তর করে।

#### গ্লোবাল কনস্ট্যান্ট:
* `NO_INFO`: `"The supplied curriculum does not provide this information."` — তথ্য না থাকলে নির্ভরযোগ্য নেতিবাচক উত্তর।
* `MESSAGES`: রিজেকশন এবং এরর স্ট্যাটাসের জন্য পূর্বনির্ধারিত স্পষ্ট বার্তা (যেমন `empty`, `out_of_scope`, `gibberish`, `no_match`, `insufficient_representation`)।

#### ফাংশনসমূহ:

##### ১. `excerpt(course, query)`
* **সিগনেচার:** `excerpt(course: dict, query: str) -> str`
* **প্যারামিটার:** `course` — সংশ্লিষ্ট কোর্স ডিকশনারি; `query` — ব্যবহারকারীর সার্চ কুয়েরি।
* **রিটার্ন:** সিলেবাসের ডেসক্রিপশন থেকে সবচেয়ে প্রাসঙ্গিক একক বাক্যটি।
* **কার্যপ্রণালী:** কোর্সের `description` কে সেমিকোলন ও দাড়ি দিয়ে আলাদা বাক্যে বিভক্ত করে। ব্যবহারকারীর কুয়েরি শব্দের সাথে যে বাক্যের সর্বাধিক শব্দ মিলে যায় (`max(sentences, key=overlap)`), সেই বাক্যটিকে এক্সসার্পট (উদ্ধৃতি) হিসেবে নির্বাচন করে।

##### ২. `format_answer(parsed, results, status)`
* **সিগনেচার:** `format_answer(parsed: ParsedQuery, results: list[dict], status: str) -> str`
* **প্যারামিটার:** `parsed` — পার্স করা কুয়েরি অবজেক্ট; `results` — রিট্রিভ করা কোর্স লিস্ট; `status` — এক্সিকিউশন স্ট্যাটাস স্ট্রিং (`ok`, `no_match` ইত্যাদি)।
* **রিটার্ন:** ব্যবহারকারীকে প্রদর্শনের উপযোগী ফরম্যাট করা ফাইনাল স্ট্রিং।
* **কার্যপ্রণালী:**
  1. যদি স্ট্যাটাস `MESSAGES`-এ থাকে, তবে সরাসরি নিরাপদ রিজেকশন বার্তা পাঠায়।
  2. **`CREDIT_QUERY`**: কোর্সের নাম, ক্রেডিট সংখ্যা এবং যদি সোর্স ফাইলে কোনো কনফ্লিক্ট থাকে (যেমন বিস্তারিত সিলেবাস ও সামারি টেবিলের অসঙ্গতি), তবে স্পষ্টভাবে উল্লেখ করে দেয়: `"CSE 4121 has 3.0 credits. Source conflict: ..."`।
  3. **`CONTACT_HOUR_QUERY`**: সাপ্তাহিক কন্টাক্ট আওয়ার ফরম্যাট করে উপস্থাপন করে।
  4. **`LAB_QUERY` ও `YEAR_TERM_QUERY`**: একাধিক ল্যাব কোর্সের তালিকা বুলেট পয়েন্ট আকারে বছর ও টার্ম সহ ডিসপ্লে করে।
  5. **`COURSE_DETAILS`**: কোর্সের ক্রেডিট, বছর, টার্ম এবং টপিকের পূর্ণাঙ্গ প্রোফাইল তুলে ধরে।
  6. **`TOPIC_SEARCH`**: শ্রেষ্ঠ ম্যাচিং কোর্স এবং সিলেবাস থেকে উদ্ধারকৃত উদ্ধৃতি (`Relevant curriculum content`) প্রদর্শন করে।

---

### ২.৫. `src/registry.py` (মডেল ফ্যাক্টরি ও লাইফসাইকেল কন্ট্রোলার)

এই ফাইলটি অ্যাপ্লিকেশন ও স্ক্রিপ্ট থেকে চারটি আলাদা মডেলকে কনফিগারেশন অনুযায়ী ইনিশিয়ালাইজ ও লোড করার ফ্যাক্টরি প্যাটার্ন নিশ্চিত করে।

#### গ্লোবাল ডিকশনারি:
* `MODEL_LABELS`: মডেল আইডি থেকে UI-তে প্রদর্শনের জন্য সুন্দর লেবেলের ম্যাপিং:
  * `"model_a"`: `"Model A — TF-IDF + Cosine"`
  * `"model_b"`: `"Model B — Pretrained Word2Vec"`
  * `"model_c"`: `"Model C — Word2Vec + BiLSTM"`
  * `"model_d"`: `"Model D — Sentence-BERT"`

#### ফাংশনসমূহ:

##### ১. `configured_vector_path()`
* **সিগনেচার:** `configured_vector_path() -> str | None`
* **প্যারামিটার:** কোনো প্যারামিটার নেই।
* **রিটার্ন:** Word2Vec মডেল ফাইলের অ্যাবসলিউট পাথ অথবা `None`।
* **কার্যপ্রণালী:** প্রথমে পরিবেশ ভ্যারিয়েবল `WORD2VEC_PATH` চেক করে। তা না থাকলে `artifacts/vector_source.json` ফাইল চেক করে এবং সংরক্ষিত ফাইলের সঠিক পাথ রিটার্ন করে।

##### ২. `create_model(model_id, courses=None, load=True, vector_path=None)`
* **সিগনেচার:** `create_model(model_id: str, courses: list = None, load: bool = True, vector_path: str = None) -> BaseRetriever`
* **প্যারামিটার:** `model_id` — কাঙ্ক্ষিত মডেল কোড (`model_a`, `model_b`, `model_c`, `model_d`); `courses` — অপশনাল কোর্স লিস্ট; `load` — প্রি-বিল্ট আর্টফ্যাক্ট লোড করবে কিনা; `vector_path` — অপশনাল কাস্টম ওয়ার্ড২ভেক পাথ।
* **রিটার্ন:** লোড করা সংশ্লিষ্ট রিট্রিভার অবজেক্ট।
* **কার্যপ্রণালী:** 
  1. ডায়নামিক ইম্পোর্ট ব্যবহার করে কেবল যে মডেলটি চাওয়া হয়েছে তার ক্লাস ইম্পোর্ট করে (ফলে অপ্রয়োজনীয় মেমরি অপচয় রোধ হয়)।
  2. `load=True` হলে চেক করে `manifest.json` ফাইল আছে কিনা। না থাকলে নির্দেশমূলক `FileNotFoundError` থ্রো করে।
  3. সংশ্লিষ্ট মডেল অবজেক্ট ইনস্ট্যানশিয়েট করে `model.load()` কল করে রেডি রিট্রিভার রিটার্ন করে।

---

### ২.৬. `src/evaluation.py` (মেট্রিক্স গণনা ও থ্রেশহোল্ড ক্যালিব্রেশন)

মডেলগুলোর কঠোর গাণিতিক মূল্যায়ন এবং নিরপেক্ষ সিদ্ধান্ত নেওয়ার জন্য প্রয়োজনীয় মেট্রিক্স ও ক্যালিব্রেশন ইঞ্জিন রয়েছে এই ফাইলে।

#### ফাংশনসমূহ:

##### ১. `ranked_codes(model, query)`
* **সিগনেচার:** `ranked_codes(model: BaseRetriever, query: str) -> tuple[list[str], float, dict]`
* **প্যারামিটার:** `model` — পরীক্ষাধীন রিট্রিভাল মডেল; `query` — ব্যবহারকারীর কুয়েরি।
* **রিটার্ন:** `(সর্টেড_কোর্স_কোডের_লিস্ট, টপ_স্কোর, ডিব্যাগ_ডিকশনারি)`।
* **কার্যপ্রণালী:** 
  1. মডেলের পার্সার দিয়ে সার্চ কুয়েরি বের করে মডেলের `score()` ফাংশন কল করে।
  2. ইনটেন্ট অনুযায়ী ল্যাব বা থিওরি কোর্স ফিল্টার করে।
  3. কোর্সগুলোকে স্কোরের নিম্নক্রম (descending order) অনুসারে সাজায় এবং টাই ব্রেকিংয়ের জন্য কোর্স কোড দিয়ে সর্ট করে।
  4. ইউনিক কোর্স কোডের র‍্যাঙ্কড তালিকা এবং টপ স্কোর রিটার্ন করে।

##### ২. `retrieval_metrics(ranked, gold)`
* **সিগনেচার:** `retrieval_metrics(ranked: list[str], gold: set[str]) -> dict`
* **প্যারামিটার:** `ranked` — মডেল কর্তৃক সাজানো কোর্স কোডের তালিকা; `gold` — সত্য উত্তরের সেট (Gold ground truth)।
* **রিটার্ন:** `{"Hit@1": float, "Hit@3": float, "MRR": float, "Recall@3": float}`।
* **কার্যপ্রণালী:**
  * **Hit@1:** প্রথম স্থানে থাকা কোর্সটি কি gold সেটে আছে? ($1.0$ অথবা $0.0$)।
  * **Hit@3:** শীর্ষ ৩টি কোর্সের মধ্যে অন্তত একটি gold কোর্স আছে কিনা?
  * **MRR (Mean Reciprocal Rank):** প্রথম সঠিক কোর্সটির র‍্যাঙ্কের বিপরীত মান: $\text{MRR} = rac{1}{\text{rank}_{\text{first}}}$। না পাওয়া গেলে $0.0$।
  * **Recall@3:** শীর্ষ ৩টির মধ্যে কয়টি gold কোর্স এসেছে তার অনুপাত: $rac{|\text{Top-3} \cap \text{Gold}|}{|\text{Gold}|}$।

##### ৩. `calibrate_threshold(model, validation_rows)`
* **সিগনেচার:** `calibrate_threshold(model: BaseRetriever, validation_rows: list[dict]) -> dict`
* **প্যারামিটার:** `model` — সংশ্লিষ্ট মডেল; `validation_rows` — কেবল ভ্যালিডেশন স্প্লিটের কুয়েরিগুলো।
* **রিটার্ন:** সর্বোত্তম থ্রেশহোল্ড, ব্যালেন্সড এক্যুরেসি এবং ম্যানিফেস্ট অডিট সহ ডিকশনারি।
* **কার্যপ্রণালী:** 
  1. সমস্ত ভ্যালিডেশন কুয়েরির টপ স্কোর বের করে সম্ভাব্য থ্রেশহোল্ড ক্যান্ডিডেট পয়েন্ট তৈরি করে।
  2. প্রতিটি ক্যান্ডিডেট থ্রেশহোল্ডের জন্য True Positive Rate (TPR) এবং True Negative Rate (TNR) গণনা করে:
     $$\text{Balanced Accuracy} = rac{\text{TPR} + \text{TNR}}{2}$$
  3. টাই হলে সিস্টেম TNR (রিজেকশন ক্ষমতা)-কে অগ্রাধিকার দেয় যাতে মিথ্যা উত্তর বা হ্যালুসিনেশন কমে।
  4. নির্বাচিত থ্রেশহোল্ড এবং ভ্যালিডেশনের সমস্ত স্যাম্পল মেটাডেটা রিটার্ন করে।

##### ৪. `evaluate_model(model, rows)`
* **সিগনেচার:** `evaluate_model(model: BaseRetriever, rows: list[dict]) -> list[dict]`
* **প্যারামিটার:** `model` — মডেল; `rows` — টেস্ট ডেটাসেটের সারিগুলো।
* **রিটার্ন:** প্রতিটি কুয়েরির বিস্তারিত পারফরম্যান্স লগ।
* **কার্যপ্রণালী:** দুটি মোডে মূল্যায়ন চালায়:
  * **`retrieval_only`**: থ্রেশহোল্ড ফিল্টারিং ছাড়া পিওর র‍্যাঙ্কিং মান কেমন।
  * **`end_to_end`**: থ্রেশহোল্ড রিজেকশন এবং পার্সার ডিসিশন সহ ব্যবহারকারীর পাওয়া ফাইনাল উত্তরের মান কেমন।

##### ৫. `summarize(details)`
* **সিগনেচার:** `summarize(details: list[dict]) -> dict`
* **প্যারামিটার:** `details` — `evaluate_model` এর আউটপুট লগ।
* **রিটার্ন:** সামগ্রিক পারফরম্যান্স সামারি (Hit@1, Hit@3, MRR, Recall@3, OOD Accuracy, FAR, FRR)।
* **কার্যপ্রণালী:** নেগেটিভ কুয়েরির জন্য False Acceptance Rate (FAR) এবং ডোমেইনের বাইরের প্রশ্নের জন্য OOD Accuracy ও ফলস রিজেকশন রেট (FRR) গড় হিসাব করে তুলে ধরে।

##### ৬. `write_csv(path, rows)`
* **সিগনেচার:** `write_csv(path: Path, rows: list[dict]) -> None`
* **প্যারামিটার:** `path` — CSV ফাইলের পাথ; `rows` — রেকর্ডের তালিকা।
* **রিটার্ন:** `None`।
* **কার্যপ্রণালী:** `csv.DictWriter` ব্যবহার করে সুন্দর হেডার সহ ইউটিএফ-৮ ফরম্যাটে CSV ফাইল লেখে।

---

### ২.৭. `src/datasets.py` (ডেটাসেট হ্যান্ডলিং ও নেগেটিভ স্যাম্পলিং)

এই মডিউলটি ট্রেনিং ও টেস্টিং ডেটার ডেটা ইন্টিগ্রিটি, নো-লিকেজ অডিট এবং BiLSTM-এর জন্য সুষম নেগেটিভ স্যাম্পলিং তৈরি নিশ্চিত করে।

#### ফাংশনসমূহ:

##### ১. `read_queries(path)`
* **সিগনেচার:** `read_queries(path: str | Path) -> list[dict]`
* **প্যারামিটার:** `path` — কুয়েরি CSV ফাইলের পাথ।
* **রিটার্ন:** প্রতিটি সারি ডিকশনারি আকারে সংরক্ষিত তালিকা।
* **কার্যপ্রণালী:** `csv.DictReader` দিয়ে ফাইল পড়ে এবং ট্রিম করা টেক্সট সম্বলিত লিস্ট প্রদান করে।

##### ২. `gold_codes(row)`
* **সিগনেচার:** `gold_codes(row: dict) -> set[str]`
* **প্যারামিটার:** `row` — ডেটাসেটের একক সারি।
* **রিটার্ন:** সত্য কোর্স কোডগুলোর সেট (যেমন `{"CSE 4121"}`)।
* **কার্যপ্রণালী:** `gold_course_code` অথবা `positive_course_code` কলাম থেকে পাইপ (`|`) চিহ্ন দিয়ে বিভক্ত করে মাল্টি-লেবেল কোড আলাদা করে সেটে রূপান্তর করে।

##### ৩. `validate_datasets(relevance, evaluation, courses)`
* **সিগনেচার:** `validate_datasets(relevance: list, evaluation: list, courses: list) -> dict`
* **প্যারামিটার:** `relevance` — রেলিভেন্স ডেটাসেট; `evaluation` — টেস্ট ডেটাসেট; `courses` — ১০৮টি কোর্সের তালিকা।
* **রিটার্ন:** স্প্লিট পরিসংখ্যান ডিকশনারি।
* **কার্যপ্রণালী (লিকেজ অডিট):**
  1. **ডুপ্লিকেট আইডি চেক:** কোনো `query_id` একের অধিকবার আছে কিনা যাচাই করে।
  2. **এক্সাক্ট কুয়েরি লিকেজ:** ট্রেইনিং কুয়েরির হুবহু টেক্সট টেস্ট সেটে আছে কিনা নিশ্চিত করে। থাকলে `ValueError` থ্রো করে।
  3. **প্যারাফ্রেজ গ্রুপ লিকেজ:** একই `group_id` বিশিষ্ট প্রশ্নের রূপান্তর ভিন্ন স্প্লিটে ছড়িয়ে আছে কিনা তা বন্ধ করে।
  4. **অজানা কোর্স চেক:** গোল্ড কোড হিসেবে এমন কোনো কোর্স কোড আছে কিনা যা সিলেবাস ডেটাবেজে নেই।

##### ৪. `make_pairs(rows, courses, lexical_model, seed=42)`
* **সিগনেচার:** `make_pairs(rows: list, courses: list, lexical_model: BaseRetriever, seed: int = 42) -> list[dict]`
* **প্যারামিটার:** `rows` — কুয়েরি সারি; `courses` — কোর্স ডেটাবেজ; `lexical_model` — হার্ড নেগেটিভ খোঁজার জন্য TF-IDF মডেল; `seed` — র‍্যান্ডম সীড।
* **রিটার্ন:** BiLSTM ট্রেইনিংয়ের জন্য লেবেলযুক্ত জোড়ার তালিকা (`query`, `document_index`, `label`, `sampling`)।
* **কার্যপ্রণালী (নেগেটিভ স্যাম্পলিং স্ট্র্যাটেজি):**
  * পজিটিভ কুয়েরির জন্য:
    * ১টি পজিটিভ পেয়ার ($\text{label} = 1$)।
    * ৩টি **TF-IDF Hard Negatives** ($\text{label} = 0$): লেক্সিক্যাল মডেলে সর্বোচ্চ স্কোর পাওয়া কিন্তু গোল্ড তালিকায় না থাকা কোর্স।
    * ২টি **Random Negatives** ($\text{label} = 0$): বাকি কোর্স থেকে দৈবচয়ন করা কোর্স।
  * নেগেটিভ/OOD কুয়েরির জন্য:
    * ৫টি **Out-of-domain Negatives** ($\text{label} = 0$)।
  * এটি নিশ্চিত করে যে নিউরাল নেটওয়ার্ক কঠিন শব্দ মিল এবং দৈব শব্দের মধ্যে পার্থক্য শিখতে পারে।

---

## ৩. কর্পাস এক্সট্র্যাকশন ও অডিট ইঞ্জিন (`src/corpus/`)

এই সাব-প্যাকেজটি কাঁচা সিলেবাস PDF ফাইলটি স্বয়ংক্রিয়ভাবে পড়ে স্ট্রাকচার্ড ডেটাবেজ রূপান্তর করে।

### ৩.১. `src/corpus/pdf_extractor.py` (PyMuPDF পেজ এক্সট্রাক্টর)

#### ফাংশন: `extract_pages(pdf_path)`
* **সিগনেচার:** `extract_pages(pdf_path: str | Path) -> list[dict]`
* **প্যারামিটার:** `pdf_path` — সিলেবাস PDF ফাইলের পাথ (`CourseContentsCSE_14_08_2022.pdf`)।
* **রিটার্ন:** `[{"page": 1, "text": "..."}, {"page": 2, "text": "..."}]` আকারের তালিকা।
* **কার্যপ্রণালী:** 
  1. `pymupdf.open(pdf_path)` দিয়ে ডকুমেন্ট ওপেন করে।
  2. প্রতি পৃষ্ঠার টেক্সট `page.get_text("text")` দিয়ে রিড করে।
  3. ১-ইন্ডেক্সড পেজ নম্বর সহ পেজ টেক্সটের তালিকা রিটার্ন করে।

---

### ৩.২. `src/corpus/corpus_builder.py` (সিলেবাস পার্সার ও অডিট পাইপলাইন)

এটি সম্পূর্ণ প্রজেক্টের ডেটা নিষ্কাশনের হৃদপিণ্ড। এটি ১১২ পৃষ্ঠার সিলেবাসের অস্বাভাবিকতা ও কাঠামোগত ত্রুটি দূর করে ১০৮টি কোর্সের ক্লিন ডেটাবেজ তৈরি করে।

#### রেগুলার এক্সপ্রেশনসমূহ:
* `HEADING`: `^([A-Z]{2,5})\s+(\d{4})(?:\s*:\s*|\s+)(.+)$` — কোর্স কোড এবং কোর্স শিরোনাম আলাদা করে। এটি কোলন থাকা বা না থাকা উভয় অবস্থাই হ্যান্ডেল করে (যেমন পেজ ১৭-এর `CSE 4245` তে কোলন ছিল না)।
* `YEAR_TERM`: `([1-4](?:st|nd|rd|th))\s*Year\s*([12](?:st|nd))\s*Term` — শিক্ষাবর্ষ ও টার্ম হেডার শনাক্ত করে।
* `OPTIONAL`: `Optional\s*-\s*(III|II|I)\b` — অপশনাল কোর্সের গ্রুপ শনাক্ত করে।

#### ফাংশনসমূহ:

##### ১. `parse_pages(pages)`
* **সিগনেচার:** `parse_pages(pages: list[dict]) -> list[dict]`
* **প্যারামিটার:** `pages` — এক্সট্র্যাক্ট করা পৃষ্ঠাসমূহের তালিকা।
* **রিটার্ন:** ১০৮টি পার্স করা কোর্সের বিস্তারিত রেকর্ডের তালিকা।
* **কার্যপ্রণালী:**
  * পেজ ধরে লাইন বাই লাইন স্ক্যান করে।
  * `Summary of` বা `Syllabus of` লাইন পেলে তৎকালীন সেকশন স্টেট আপডেট করে।
  * সামারি টেবিলের লাইনগুলোকে সিলেবাসের ডেসক্রিপশনে মিশে যাওয়া থেকে আটকে দেয় (`summary` ফ্ল্যাগ দিয়ে)।
  * **নেস্টেড ফাংশন `finish()`**:
    * একটি কোর্সের সমস্ত লাইন জমা হওয়ার পর ক্রেডিট (`Credits: X.X`) এবং কন্টাক্ট আওয়ার (`Contact Hours: X.X`) এক্সট্র্যাক্ট করে।
    * ডেসক্রিপশন থেকে ক্রেডিট ও আওয়ার টেক্সট ক্লিন করে পিওর সিলেবাস টেক্সট আলাদা করে।
    * টাইটেল এবং ডেসক্রিপশন পর্যবেক্ষণ করে `course_type` নির্ধারণ করে (`Laboratory`, `Project`, অথবা `Theory`)।
    * ল্যাব যদি খুব ছোট হয় তবে `generic_lab` চিহ্নিত করে।
    * রিট্রিভালের সুবিধার্থে ডাবল-ওয়েটেড টাইটেল সহ `search_text` প্রস্তুত করে: `"{course_code} {title} {title} {description}"`।
    * ইউনিক `record_id` তৈরি করে: `"{course_code}_{year}_{term}"`।

##### ২. `audit_summary_tables(pdf_path, courses)`
* **সিগনেচার:** `audit_summary_tables(pdf_path: Path, courses: list[dict]) -> None`
* **প্যারামিটার:** `pdf_path` — PDF ফাইলের পাথ; `courses` — পার্স করা কোর্স অবজেক্টের তালিকা।
* **রিটার্ন:** `None` (ইন-প্লেস আপডেট করে)।
* **কার্যপ্রণালী (স্পেশিয়াল টেবিল এক্সট্র্যাকশন ও অডিট):**
  * PyMuPDF-এর `page.get_text("words")` ব্যবহার করে কো-অর্ডিনেট ভিত্তিক শব্দ অবস্থান নির্ণয় করে।
  * কলামের জ্যামিতিক স্থানাঙ্ক ($x$-কোঅর্ডিনেট) ধরে লেকচার (L), প্র্যাক্টিক্যাল (P) এবং ক্রেডিট কলাম থেকে ভগ্নাংশ (`fractions.Fraction`) আকারে মান পড়ে।
  * বিস্তারিত সিলেবাসের মানের সাথে সামারি টেবিলের মান তুলনা করে।
  * যদি সিলেবাসের সাথে সামারি টেবিলের অসঙ্গতি থাকে (যেমন `HUM 4207`-এ সিলেবাসে ২.০ কিন্তু সামারি টেবিলে ৩.০; অথবা `EEE 2118`-এ কন্টাক্ট আওয়ারে অসামঞ্জস্য), তবে তা ওভাররাইট না করে `source_conflicts` তালিকায় পেজ নম্বর সহ লিপিবদ্ধ করে।

##### ৩. `build_corpus(pdf_path, output)`
* **সিগনেচার:** `build_corpus(pdf_path: str | Path, output: str | Path) -> dict`
* **প্যারামিটার:** `pdf_path` — মূল PDF ফাইল; `output` — ডেটা সেভ করার আউটপুট ফোল্ডার (`data/processed`)।
* **রিটার্ন:** কর্পাস অডিট পরিসংখ্যান ডিকশনারি।
* **কার্যপ্রণালী:**
  1. `extract_pages()` কল করে টেক্সট বের করে।
  2. `parse_pages()` দিয়ে ১০৮টি কোর্স পার্স করে।
  3. `audit_summary_tables()` দিয়ে টেবিল অডিট করে।
  4. `courses.json` এবং `courses.csv` ফাইলে ডেটা সেভ করে।
  5. `corpus_statistics.json` ফাইলে ইউনিক কোড, থিওরি/ল্যাব সংখ্যা, মিসিং ফিল্ড ও কনফ্লিক্টের লগ লিখে পরিসংখ্যান রিটার্ন করে।

---

## ৪. রিট্রিভাল মডেল আর্কিটেকচার (`src/models/`)

এই প্যাকেজের অধীনে প্রকল্পের চারটি মডেলের গাণিতিক কাঠামো, এমবেডিং তৈরি, ইনডেক্সিং এবং সার্চ অ্যালগরিদম বাস্তবায়িত হয়েছে।

### ৪.১. `src/models/base.py` (মডেলের অ্যাবস্ট্রাক্ট বেস ক্লাস ও পাইপলাইন)

এই ফাইলটি একটি সাধারণ কাঠামো (`BaseRetriever`) প্রদান করে যা চারটি মডেলের মধ্যে কোডের পুনরাবৃত্তি দূর করে এবং প্রতিটি মডেলের জন্য অভিন্ন রিট্রিভাল ইন্টারফেস নিশ্চিত করে।

#### ক্লাস: `BaseRetriever`
* **অ্যাট্রিবিউটসমূহ:**
  * `courses`: ১০৮টি কোর্সের তালিকা।
  * `threshold`: অপটিমাল ডিসিশন কাটঅফ স্কোর (যা `artifacts/thresholds.json` থেকে লোড হয়)।
  * `calibrated`: মডেলটি ভ্যালিডেশন সেটে ক্যালিব্রেট করা হয়েছে কিনা তার বুলিয়ান ফ্ল্যাগ।
  * `directory`: মডেলের আর্টফ্যাক্ট সংরক্ষণের ফোল্ডার পাথ (`artifacts/{model_id}`).
  * `fingerprint`: বর্তমান সিলেবাস ডেটাবেজের SHA-256 হ্যাশ।
  * `parser`: `QueryParser` ক্লাসের অবজেক্ট।

#### মেথডসমূহ:

##### ১. `__init__(self, courses, threshold=None, directory=None)`
* **প্যারামিটার:** `courses` — কোর্স তালিকা; `threshold` — অপশনাল কাটঅফ; `directory` — অপশনাল ডিরেক্টরি।
* **কার্যপ্রণালী:** অবজেক্ট ইনিশিয়ালাইজ করে, `QueryParser` ইনস্ট্যান্স তৈরি করে এবং `artifacts/thresholds.json` বিদ্যমান থাকলে স্বয়ংক্রিয়ভাবে ক্যালিব্রেটেড থ্রেশহোল্ড সেট করে নেয়।

##### ২. `save_manifest(self, **extra)`
* **প্যারামিটার:** `extra` — অতিরিক্ত মেটাডেটা কি-ভ্যালু।
* **কার্যপ্রণালী:** মডেলের ডিরেক্টরিতে একটি `manifest.json` ফাইল তৈরি করে। এতে মডেল আইডি, ডেটাবেজ ফিঙ্গারপ্রিন্ট, ডিফল্ট ও ক্যালিব্রেটেড থ্রেশহোল্ড এবং এক্সট্রা মেটাডেটা লিখে রাখে।

##### ৩. `check_manifest(self)`
* **কার্যপ্রণালী:** `manifest.json` লোড করে যাচাই করে যে বর্তমান সিলেবাস ডেটাবেজের সাথে ইনডেক্স তৈরির সময়কার ডেটাবেজের হ্যাশ মিলছে কিনা (`manifest["corpus_hash"] == self.fingerprint`)। অমিল থাকলে `ValueError("Corpus changed; rebuild the index.")` তুলে সিস্টেম বন্ধ করে দেয়। এটি স্টেল বা মিসম্যাচড ইনডেক্স থেকে ভুল উত্তর রোধ করে।

##### ৪. `score(self, query) -> tuple[np.ndarray, dict]`
* **অ্যাবস্ট্রাক্ট মেথড:** প্রতিটি চাইল্ড ক্লাসকে এই মেথডটি বাধ্যতামূলকভাবে ইমপ্লিমেন্ট করতে হয়। এটি কুয়েরির বিপরীতে ১০৮টি কোর্সের স্কোর অ্যারে এবং ডিব্যাগ তথ্য রিটার্ন করে।

##### ৫. `retrieve(self, query, top_k=3) -> dict`
* **প্যারামিটার:** `query` — ব্যবহারকারীর প্রশ্ন; `top_k` — সর্বোচ্চ কতটি রেজাল্ট প্রদর্শিত হবে (ডিফল্ট ৩)।
* **রিটার্ন:** `{"query": ..., "status": ..., "parsed": ..., "results": ..., "answer": ..., "debug": ...}` সম্বলিত পূর্ণাঙ্গ রেসপন্স ডিকশনারি।
* **কার্যপ্রণালী (টু-স্টেজ রিট্রিভাল পাইপলাইন):**
  1. **স্টেজ ১: পার্সিং ও ডিটারমিনিস্টিক মেটাডেটা ফিল্টারিং:** কুয়েরি পার্স করে যদি `EMPTY`, `GIBBERISH`, `OUT_OF_SCOPE` ইত্যাদি পায়, তবে মডেল রান না করেই তাৎক্ষণিক নিরাপদ রিজেকশন উত্তর দেয়। যদি নির্দিষ্ট কোর্স কোড (যেমন `CSE 4121`) থাকে, তবে মেটাডেটা দিয়ে সরাসরি রেজাল্ট ফেরত দেয়।
  2. **স্টেজ ২: গাণিতিক স্কোরিং ও র‍্যাঙ্কিং:** মডেলের `score()` ফাংশন কল করে ১০৮টি কোর্সের স্কোর বের করে।
  3. **OOV ও থ্রেশহোল্ড গেটিং:** যদি কুয়েরির সব শব্দ আউট-অব-ভোকাবুলারি (OOV) হয়, তবে `insufficient_representation` বা `no_match` রিটার্ন করে। সর্বোচ্চ স্কোর যদি `self.threshold`-এর নিচে থাকে, তবে সিস্টেম মিথ্যা উত্তর দেওয়ার বদলে `no_match` বলে কুয়েরি বাতিল করে।
  4. **সর্টিং ও অ্যান্সার ফরম্যাটিং:** শীর্ষ স্কোর পাওয়া কোর্সগুলোর উদ্ধৃতি তৈরি করে `format_answer` এর মাধ্যমে ফাইনাল উত্তর প্রস্তুত করে।

##### ৬. `_results(self, pairs, parsed) -> list[dict]`
* **প্যারামিটার:** `pairs` — `[(কোর্স, স্কোর)]` টিউপলের তালিকা; `parsed` — পার্স করা কুয়েরি।
* **রিটার্ন:** ফরম্যাট করা রেজাল্ট ডিকশনারির তালিকা।
* **কার্যপ্রণালী:** কোর্সের মেটাডেটার সাথে সিলেবাসের প্রাসঙ্গিক উদ্ধৃতি (`matched_excerpt`) এবং স্কোর যুক্ত করে ডিকশনারি তৈরি করে।

---

### ৪.২. `src/models/word_vectors.py` (ওয়ার্ড ভেক্টর ও OOV হ্যান্ডলিং)

ওয়ার্ড২ভেক ভেক্টর মেমরিতে লোড করা এবং BiLSTM-এর জন্য OOV শব্দ বাদ দিয়ে সিকোয়েন্স ভেক্টর তৈরির কাজ করে এই ফাইল।

#### ফাংশনসমূহ:

##### ১. `load_word_vectors(path=None)`
* **সিগনেচার:** `load_word_vectors(path: str = None) -> KeyedVectors`
* **প্যারামিটার:** `path` — অপশনাল মডেল ফাইল পাথ।
* **রিটার্ন:** Gensim `KeyedVectors` অবজেক্ট।
* **কার্যপ্রণালী:** 
  * `@lru_cache(maxsize=2)` দ্বারা ক্যাশড থাকে, ফলে বারবার ডিস্ক থেকে রিড করতে হয় না।
  * যদি পাথ দেওয়া থাকে এবং ফাইল এক্সটেনশন `.kv` হয়, তবে `KeyedVectors.load(..., mmap="r")` দিয়ে মেমরি-ম্যাপড মোডে তাৎক্ষণিক লোড করে।
  * `.bin` বা `.bin.gz` হলে `load_word2vec_format(..., binary=True)` ব্যবহার করে।
  * অফলাইন মোডে ফাইল না পেলে পরিষ্কার নির্দেশমূলক `FileNotFoundError` দেয়।

##### ২. `vector_key(token, vectors)`
* **সিগনেচার:** `vector_key(token: str, vectors: KeyedVectors) -> str | None`
* **প্যারামিটার:** `token` — একটি শব্দ; `vectors` — ওয়ার্ড২ভেক মডেল।
* **রিটার্ন:** মডেলে বিদ্যমান সঠিক কেস স্ট্রিং অথবা `None`।
* **কার্যপ্রণালী:** কেস-ইনসেনসিটিভ লুকআপ চালায়: প্রথমে মূল শব্দ (`token`), না পেলে সব বড় হাতের (`token.upper()`), না পেলে ক্যাপিটালাইজড (`token.capitalize()`) চেক করে। যেমন `"rsa"` না পেলে `"RSA"` খুঁজে নেয়।

##### ৩. `sequence_vectors(text, vectors, max_length=256)`
* **সিগনেচার:** `sequence_vectors(text: str, vectors: KeyedVectors, max_length: int = 256) -> tuple[np.ndarray, list[str], list[str]]`
* **প্যারামিটার:** `text` — ইনপুট টেক্সট; `vectors` — ওয়ার্ড ভেক্টর মডেল; `max_length` — সর্বোচ্চ দৈর্ঘ্য (ডিফল্ট ২৫৬)।
* **রিটার্ন:** `(ভেক্টর_ম্যাট্রিক্স, কাভার্ড_টোকেন_লিস্ট, ওওভি_টোকেন_লিস্ট)`।
* **কার্যপ্রণালী (কমপ্যাক্ট অ্যাওয়ে মেকানিজম):**
  * টেক্সট থেকে প্রথম ২৫৬টি টোকেন নেয়।
  * প্রতিটি টোকেনের জন্য `vector_key` দিয়ে ভেক্টর খোঁজে।
  * যে শব্দগুলো ওয়ার্ড২ভেকে নেই (OOV), সেগুলোর জন্য শূন্য ভেক্টর না বসিয়ে **বাদ দিয়ে বাকি জ্ঞাত শব্দগুলোর ক্রম ঠিক রেখে অ্যারে তৈরি করে**।
  * **কেন এটি গুরুত্বপূর্ণ:** শূন্য ভেক্টর যোগ করলে BiLSTM-এর ব্যাকওয়ার্ড পাস এবং ম্যাক্স/লাস্ট হিডেন স্টেট করাপ্ট বা বিকৃত হয়ে যায়। তাই OOV কমপ্যাক্ট করে বাদ দিলে নিউরাল নেটওয়ার্ক শুধুমাত্র অর্থপূর্ণ শব্দের ওপর ফোকাস করতে পারে।

---

### ৪.৩. `src/models/model_a_tfidf.py` (মডেল এ: TF-IDF + Cosine Retriever)

লেক্সিক্যাল ও কি-ওয়ার্ড ভিত্তিক রিট্রিভাল নিশ্চিত করে এই মডেলটি।

#### ক্লাস: `TfidfRetriever(BaseRetriever)`
* **মডেল আইডি:** `"model_a"`
* **ডিফল্ট থ্রেশহোল্ড:** `0.12`

#### মেথডসমূহ:

##### ১. `build(self)`
* **কার্যপ্রণালী:**
  * Scikit-Learn-এর `TfidfVectorizer` ইনিশিয়ালাইজ করে:
    * `tokenizer=lexical_tokens`: কাস্টম লেম্যাটাইজার ও স্টপওয়ার্ড ফিল্টার।
    * `ngram_range=(1, 2)`: ইউনিগ্রাম ও বাইগ্রাম ফিচার (যেমন `binary` এবং `binary search` উভয়ই আলাদা ফিচার)।
    * `sublinear_tf=True`: সাব-লিনিয়ার টার্ম ফ্রিকোয়েন্সি স্কেলিং: $1 + \ln(\text{tf})$।
    * `norm="l2"`: প্রতিটি ডকুমেন্ট ভেক্টর ইউনিট দৈর্ঘ্যে নরমালাইজড।
  * ১০৮টি কোর্সের `search_text` দিয়ে ভেক্টরাইজার ফিট ও ট্রান্সফর্ম করে $108 	imes 9786$ স্পার্স CSR ম্যাট্রিক্স তৈরি করে।
  * `artifacts/model_a/index.joblib` ফাইলে ভেক্টরাইজার এবং ম্যাট্রিক্স সংরক্ষণ করে এবং ম্যানিফেস্ট সেভ করে।

##### ২. `load(self)`
* **কার্যপ্রণালী:** ম্যানিফেস্ট যাচাই করে `index.joblib` থেকে ভেক্টরাইজার এবং স্পার্স ম্যাট্রিক্স মেমরিতে লোড করে।

##### ৩. `score(self, query) -> tuple[np.ndarray, dict]`
* **কার্যপ্রণালী:**
  * কুয়েরিটিকে `self.vectorizer.transform([query])` দিয়ে স্পার্স ভেক্টরে রূপান্তর করে।
  * ডট প্রোডাক্টের মাধ্যমে একযোগে ১০৮টি কোর্সের কসাইন সিমিলারিটি গণনা করে:
    $$\mathbf{s} = \mathbf{M} \mathbf{q}^T \in \mathbb{R}^{108}$$
  * কুয়েরির মধ্যে কোন কোন টিএফ-আইডিএফ শব্দের ওজন সবচেয়ে বেশি ছিল তার শীর্ষ ১২টি শব্দ ডিব্যাগ ডিকশনারিতে রিটার্ন করে।

---

### ৪.৪. `src/models/model_b_word2vec.py` (মডেল বি: TF-IDF Weighted Word2Vec)

প্রি-ট্রেইন্ড গুগল নিউজ ৩০০-মাত্রার ওয়ার্ড এম্বেডিং এবং টিএফ-আইডিএফ ওয়েটেড সেন্ট্রয়েড ভেক্টর ভিত্তিক সিম্যান্টিক রিট্রিভার।

#### ক্লাস: `Word2VecRetriever(BaseRetriever)`
* **মডেল আইডি:** `"model_b"`
* **ডিফল্ট থ্রেশহোল্ড:** `0.45`

#### মেথডসমূহ:

##### ১. `__init__(self, courses, vectors=None, vector_path=None, **kwargs)`
* **কার্যপ্রণালী:** প্যারেন্ট ক্লাস ইনিশিয়ালাইজ করে এবং `load_word_vectors()` দিয়ে ৩০০-মাত্রার শব্দ ভেক্টর লোড করে।

##### ২. `embed(self, text) -> tuple[np.ndarray, list[str], list[str]]`
* **প্যারামিটার:** `text` — ইনপুট ডকুমেন্ট বা কুয়েরি।
* **রিটার্ন:** `(সেন্ট্রয়েড_ভেক্টর, কাভার্ড_শব্দ, ওওভি_শব্দ)`।
* **কার্যপ্রণালী (TF-IDF ওয়েটেড সেন্ট্রয়েড ও আনসিন টার্ম স্মুথিং):**
  * টেক্সটের প্রতিটি শব্দের ফ্রিকোয়েন্সি গণনা করে।
  * যদি শব্দটি ট্রেনিং কর্পাসে থাকে, তবে তার সংরক্ষিত IDF মান ব্যবহার করে।
  * **আনসিন টার্ম স্মুথিং:** যদি কুয়েরির কোনো শব্দ কর্পাসে না থাকে কিন্তু প্রি-ট্রেইন্ড Word2Vec-এ থাকে (যেমন ব্যবহারকারী লিখল "encryption", কিন্তু সিলেবাসে শব্দটি নেই), তবে সিস্টেম তার জন্য স্মুথড আইডিএফ দেয়:
    $$\text{IDF}_{\text{unseen}} = \ln(1 + N) + 1, \quad N = 108$$
  * প্রতিটি শব্দের জন্য ওজন হিসাব করে: $w_i = (1 + \ln(\text{tf}_i)) 	imes \text{IDF}_i$।
  * ভারযুক্ত ভেক্টরের যোগফল বের করে $L_2$ নরম্যালাইজ করে একক দৈর্ঘ্যের সেন্ট্রয়েড ভেক্টর তৈরি করে:
    $$\mathbf{v} = rac{\sum w_i \mathbf{e}_i}{\max(\sum w_i, 10^{-12})}, \quad \hat{\mathbf{v}} = rac{\mathbf{v}}{\|\mathbf{v}\|_2} \in \mathbb{R}^{300}$$

##### ৩. `build(self)`
* **কার্যপ্রণালী:** ১০৮টি কোর্সের সেন্ট্রয়েড ভেক্টর তৈরি করে $108 	imes 300$ আকারের ডেন্স ম্যাট্রিক্স তৈরি করে `document_vectors.npy` ফাইলে সংরক্ষণ করে।

##### ৪. `load(self)`
* **কার্যপ্রণালী:** সেভ করা `vectorizer.joblib` এবং `document_vectors.npy` অ্যারে লোড করে।

##### ৫. `score(self, query) -> tuple[np.ndarray, dict]`
* **কার্যপ্রণালী:** কুয়েরির সেন্ট্রয়েড ভেক্টর তৈরি করে ম্যাট্রিক্স মাল্টিপ্লিকেশন (`self.matrix @ vector`) চালিয়ে ১০৮টি কোর্সের কসাইন সিমিলারিটি স্কোর গণনা করে।

---

### ৪.৫. `src/models/model_c_bilstm.py` (মডেল সি: Deep BiLSTM + MLP Network)

এটি একটি ডিপ লার্নিং ভিত্তিক রিট্রিভাল মডেল। এটি দ্বি-মুখী এলএসটিএম (BiLSTM) দিয়ে কুয়েরি ও ডকুমেন্টের শব্দের ক্রম ও কনটেক্সট এনকোড করে এবং একটি মাল্টি-লেয়ার পারসেপ্ট্রন (MLP) দিয়ে তাদের রেলিভেন্স সম্ভাবনা নির্ণয় করে।

#### ১. ক্লাস: `RelevanceNetwork(nn.Module)` (PyTorch নিউরাল নেটওয়ার্ক)
* **আর্কিটেকচার:**
  * **ইনপুট সাইজ:** ৩০০ (Word2Vec ডাইমেনশন)।
  * **হিডেন সাইজ:** ১২৮ (BiLSTM হওয়ার কারণে আউটপুট $128 	imes 2 = 256$)।
  * **লেয়ার:** ১টি দ্বি-মুখী LSTM (`bidirectional=True`, `batch_first=True`)।
  * **MLP ক্লাসিফায়ার:**
    * $\text{Linear}(1024 \to 128)$
    * $\text{ReLU}()$ অ্যাক্টিভেশন
    * $\text{Dropout}(p = 0.3)$
    * $\text{Linear}(128 \to 1)$ (লজিটের জন্য)

##### মেথডসমূহ:
* **`encode(self, sequences) -> torch.Tensor`**:
  * বিভিন্ন দৈর্ঘ্যের টেনসর সিকোয়েন্স নিয়ে `pad_sequence` দিয়ে প্যাডিং করে।
  * `pack_padded_sequence(enforce_sorted=False)` দিয়ে প্যাক করে LSTM-এ পাঠায়। এটি প্যাডিং টোকেনগুলোকে গণনা থেকে সম্পূর্ণ উপেক্ষা করে।
  * ফরোয়ার্ড ও ব্যাকওয়ার্ড হিডেন স্টেটের শেষ টাইম-স্টেপ কনক্যাটেনেট করে ২৫৬-মাত্রার রিপ্রেজেন্টেশন তৈরি করে:
    $$\mathbf{h} = [\overrightarrow{\mathbf{h}}_T \,;\, \overleftarrow{\mathbf{h}}_1] \in \mathbb{R}^{256}$$
* **`pair_logits(self, q, d) -> torch.Tensor`**:
  * কুয়েরি ভেক্টর $\mathbf{q} \in \mathbb{R}^{256}$ এবং ডকুমেন্ট ভেক্টর $\mathbf{d} \in \mathbb{R}^{256}$ এর মধ্যে ৪-উপাদান বিশিষ্ট ইন্টারঅ্যাকশন ফিচার তৈরি করে:
    $$\mathbf{x}_{\text{pair}} = [\mathbf{q} \,;\, \mathbf{d} \,;\, |\mathbf{q} - \mathbf{d}| \,;\, \mathbf{q} \odot \mathbf{d}] \in \mathbb{R}^{1024}$$
  * এই ১০২৪-মাত্রার ভেক্টরটিকে MLP ক্লাসিফায়ারে পাঠিয়ে ফাইনাল লজিট (লগ-অডস) বের করে।
* **`forward(self, queries, documents) -> torch.Tensor`**:
  * ব্যাচের সমস্ত কুয়েরি ও ডকুমেন্ট এনকোড করে `pair_logits` এর মাধ্যমে লজিট রিটার্ন করে।

#### ২. ক্লাস: `BiLSTMRetriever(BaseRetriever)`
* **মডেল আইডি:** `"model_c"`
* **ডিফল্ট থ্রেশহোল্ড:** `0.5`

##### মেথডসমূহ:
* **`tensor(self, text) -> torch.Tensor`**: `sequence_vectors` কল করে টেক্সটকে ফ্লোট৩২ PyTorch টেনসরে রূপান্তর করে।
* **`cache_documents(self)`**: ট্রেইনিং শেষে বা ইনডেক্সিংয়ের সময় ১০৮টি কোর্সের সিলেবাস এনকোড করে ২৫৬-মাত্রার এমবেডিং ম্যাট্রিক্স তৈরি করে মেমরি ও ডিস্কে ক্যাশ করে (`document_embeddings.npy`)। এর ফলে সার্চের সময় প্রতিবার কোর্স এনকোড করতে হয় না; তাৎক্ষণিক সার্চ সম্পন্ন হয়।
* **`save(self)` ও `load(self)`**: মডেলের ওজন (`weights.pt`), ডকুমেন্ট এমবেডিং এবং কনফিগারেশন ডিস্কে সেভ এবং লোড করে।
* **`score(self, query) -> tuple[np.ndarray, dict]`**:
  * কুয়েরিটিকে এনকোড করে $1 	imes 256$ ভেক্টর তৈরি করে।
  * তাকে প্রসারিত করে $108 	imes 256$ করে ক্যাশড কোর্স এমবেডিংয়ের সাথে `pair_logits` মূল্যায়ন করে।
  * আউটপুট লজিটের ওপর সিগময়েড অ্যাক্টিভেশন প্রয়োগ করে ১০৮টি কোর্সের রেলিভেন্স প্রোবাবিলিটি $\in [0, 1]$ রিটার্ন করে:
    $$P(\text{Relevant} \mid q, d) = \sigma(\text{logits}) = rac{1}{1 + e^{-\text{logits}}}$$

---

### ৪.৬. `src/models/model_d_sbert.py` (মডেল ডি: Sentence-BERT Transformer)

মডার্ন ডিপ কনটেক্সচুয়াল ট্রান্সফরমার আর্কিটেকচার ভিত্তিক মডেল।

#### ফাংশন: `load_encoder(model_name)`
* **সিগনেচার:** `load_encoder(model_name: str) -> SentenceTransformer`
* **কার্যপ্রণালী:** `@lru_cache(maxsize=2)` ডেকোরেটর সহ `sentence_transformers.SentenceTransformer` লোড করে CPU মোডে প্রস্তুত করে।

#### ক্লাস: `SentenceBERTRetriever(BaseRetriever)`
* **মডেল আইডি:** `"model_d"`
* **ডিফল্ট থ্রেশহোল্ড:** `0.40`
* **মডেলের নাম:** `"sentence-transformers/all-MiniLM-L6-v2"` (৬টি ট্রান্সফরমার লেয়ার, ৩৮৪ হিডেন ডাইমেনশন)।

#### মেথডসমূহ:

##### ১. `build(self)`
* **কার্যপ্রণালী:**
  * ১০৮টি কোর্সের `search_text` নিয়ে ট্রান্সফরমার এনকোডার দিয়ে একযোগে এনকোড করে:
    `self.encoder.encode(..., normalize_embeddings=True, batch_size=16)`
  * $108 	imes 384$ ডাইমেনশনের নরম্যালাইজড ডেন্স ম্যাট্রিক্স তৈরি করে `document_embeddings.npy` ফাইলে সংরক্ষণ করে।
  * সিলেবাসের লম্বা কোর্সের ক্ষেত্রে ট্রান্সফরমারের ২৫৬ টোকেন লিমিটের কারণে কয়টি ডকুমেন্ট ট্রাঙ্কেট (কাটা) হয়েছে তা ম্যানিফেস্টে লিখে রাখে (২৩টি ডকুমেন্ট ট্রাঙ্কেটেড হয়েছিল)।

##### ২. `load(self)`
* **কার্যপ্রণালী:** ম্যানিফেস্ট অডিট করে সেভ করা `document_embeddings.npy` ম্যাট্রিক্স লোড করে।

##### ৩. `score(self, query) -> tuple[np.ndarray, dict]`
* **কার্যপ্রণালী:**
  * ব্যবহারকারীর কুয়েরিকে এনকোড করে ৩৮৪-মাত্রার নরম্যালাইজড ভেক্টর $\mathbf{q} \in \mathbb{R}^{384}$ বের করে।
  * ডেন্স ম্যাট্রিক্স ডট প্রোডাক্টের মাধ্যমে সমস্ত কোর্সের কসাইন সিমিলারিটি বের করে:
    $$\mathbf{s} = \mathbf{M} \mathbf{q} \in \mathbb{R}^{108}$$

---

## ৫. অটোমেশন ও এক্সিকিউশন স্ক্রিপ্টস (`scripts/`)

এই ডিরেক্টরির স্ক্রিপ্টগুলো কমান্ড লাইন থেকে সম্পূর্ণ প্রজেক্টের লাইফসাইকেল (কর্পাস তৈরি, অ্যাসেট ডাউনলোড, ইনডেক্সিং, ট্রেইনিং, থ্রেশহোল্ড টিউনিং এবং বেঞ্চমার্কিং) পরিচালনা করে।

### ৫.১. `scripts/build_corpus.py` (স্বয়ংক্রিয় কর্পাস নিষ্কাশন স্ক্রিপ্ট)
* **উদ্দেশ্য:** কাঁচা সিলেবাস PDF ফাইল থেকে `courses.json`, `courses.csv` এবং `corpus_statistics.json` ফাইল তৈরি করা।
* **ফাংশন: `main()`**
  * **আর্গুমেন্ট পার্সিং:** `--pdf`: কাস্টম PDF ফাইলের পাথ নির্ধারণ করার অপশন।
  * **কার্যপ্রণালী:** 
    1. টার্গেট পাথ `data/raw/CourseContentsCSE_14_08_2022.pdf` চেক করে। যদি ইউজার পাথ না দেয় তবে প্যারেন্ট ফোল্ডারে `CourseContentsCSE*.pdf` খুঁজে কপি করে আনে।
    2. `src.corpus.corpus_builder.build_corpus()` কল করে এবং জেনারেট হওয়া পরিসংখ্যান কনসোলে প্রিন্ট করে।

---

### ৫.২. `scripts/prepare_assets.py` (অ্যাসেট ও মডেল ডাউনলোডার)
* **উদ্দেশ্য:** এক্সটার্নাল ডিপেনডেন্সি যেমন NLTK WordNet এবং Google News Word2Vec মডেল অফলাইন ব্যবহারের জন্য আগে থেকেই প্রস্তুত করা।
* **ফাংশন: `main()`**
  * **আর্গুমেন্ট পার্সিং:** 
    * `--word2vec`: মডেলের ধরন (`slim`, `full`, বা `local`)। ডিফল্ট `slim`।
    * `--path`: লোকাল ওয়ার্ড২ভেক ফাইলের পাথ।
    * `--skip-sbert`: সেন্টেন্স-বার্ট প্রি-ডাউনলোড স্কিপ করার ফ্ল্যাগ।
    * `--skip-wordnet`: NLTK প্যাকেজ ডাউনলোড স্কিপ করার ফ্ল্যাগ।
  * **কার্যপ্রণালী:**
    1. NLTK-এর `wordnet` এবং `omw-1.4` ডাউনলোড করে `.cache/nltk` ফোল্ডারে রাখে।
    2. `word2vec-slim` (প্রায় ২৬৪ মেগাবাইট এবং ৩০০,০০০ ইংরেজি শব্দের ৩-মিলিয়ন ভেক্টরের সাবসেট) গিটহাব থেকে ডাউনলোড করে `.cache/word2vec/` ফোল্ডারে রাখে।
    3. `artifacts/vector_source.json` ফাইলে মডেলের পাথ, শব্দভাণ্ডার সাইজ ও SHA-256 হ্যাশ লিখে রাখে।
    4. Sentence-BERT মডেল লোড করে লোকাল ক্যাশে নিশ্চিত করে। এর ফলে পরবর্তীতে UI চালনার সময় কোনো ইন্টারনেট কানেকশন বা ডাউনলোডের প্রয়োজন হয় না।

---

### ৫.৩. `scripts/build_all_indexes.py` (ইনডেক্স প্রি-কম্পিউট ইঞ্জিন)
* **উদ্দেশ্য:** চারটি মডেলের ইনডেক্স আগে থেকেই গণনা করে ডিস্কে সেভ করা যাতে লাইভ অ্যাপে কোনো ল্যাগ ছাড়া তাৎক্ষণিক সার্চ পাওয়া যায়।
* **ফাংশন: `main()`**
  * **আর্গুমেন্ট পার্সিং:** `--models`: কোন কোন মডেল বিল্ড করতে হবে (যেমন `a b c d`); `--word2vec-path`: কাস্টম ভেক্টর পাথ।
  * **কার্যপ্রণালী:**
    1. লুপ চালিয়ে প্রতিটি মডেলের জন্য `create_model(model_id, load=False)` কল করে।
    2. Model A, B এবং D এর ক্ষেত্রে `model.build()` কল করে যথাক্রমে TF-IDF CSR ম্যাট্রিক্স, Word2Vec সেন্ট্রয়েড ম্যাট্রিক্স এবং Sentence-BERT এমবেডিং ম্যাট্রিক্স জেনারেট করে।
    3. Model C এর ক্ষেত্রে যদি ট্রেইন করা ওজন বিদ্যমান থাকে, তবে `model.cache_documents()` দিয়ে ১০৮টি কোর্সের রিপ্রেজেন্টেশন ক্যাশ করে।
    4. `artifacts/build_status.json` ফাইলে কোন কোন মডেল সফলভাবে রেডি হয়েছে তা লগ করে রাখে।

---

### ৫.৪. `scripts/train_model_c.py` (BiLSTM নিউরাল ট্রেনিং স্ক্রিপ্ট)
* **উদ্দেশ্য:** ডিপ লার্নিং মডেল সি-এর দ্বি-মুখী এলএসটিএম এবং এমএলপি ক্লাসিফায়ারকে পজিটিভ ও হার্ড নেগেটিভ পেয়ারের ওপর ব্যাকপ্রোপাগেশনের মাধ্যমে ট্রেইন করা।
* **ফাংশন: `main()`**
  * **আর্গুমেন্ট পার্সিং:** `--epochs` (ডিফল্ট ১৫), `--batch-size` (ডিফল্ট ১৬), `--hidden-size` (ডিফল্ট ১২৮), `--max-length` (ডিফল্ট ২৫৬), `--seed` (ডিফল্ট ৪২), `--word2vec-path`।
  * **কার্যপ্রণালী:**
    1. **ডিটারমিনিজম ও রিপ্রোডিউসিবিলিটি:** `torch.manual_seed`, `np.random.seed` এবং `torch.use_deterministic_algorithms(True)` সেট করে যাতে প্রতিবার একই ফলাফল পাওয়া যায়।
    2. **ডেটা বিভাজন ও লিকেজ অডিট:** `relevance_queries.csv` থেকে ট্রেন ও ভ্যালিডেশন কুয়েরি আলাদা করে এবং কোনো গ্রুপ লিকেজ নেই তা নিশ্চিত করে।
    3. **নেগেটিভ স্যাম্পলিং:** `make_pairs()` দিয়ে ট্রেনিং ডেটার জন্য ১টি পজিটিভের বিপরীতে ৩টি হার্ড নেগেটিভ ও ২টি র‍্যান্ডম নেগেটিভ জোড়া তৈরি করে।
    4. **ইমব্যালেন্সড লস ফাংশন:** পজিটিভ ক্লাসের ঘাটতি পূরণে `torch.nn.BCEWithLogitsLoss(pos_weight=...)` ব্যবহার করে, যেখানে $w_{\text{pos}} pprox 4.85$।
    5. **নেস্টেড ফাংশন `batch_loss(batch)`:** ব্যাচের কুয়েরি ও ডকুমেন্ট টেনসর নিয়ে নেটওয়ার্কে পাস করে লজিট বের করে এবং ওয়েটেড বাইনারি ক্রস এন্ট্রপি লস গণনা করে।
    6. **ট্রেনিং লুপ ও আর্লি স্টপিং:** প্রতিটি এপোকে `optimizer.zero_grad()`, `loss.backward()`, গ্র্যাডিয়েন্ট ক্লিপিং (`clip_grad_norm_ = 1.0`), এবং `optimizer.step()` এক্সিকিউট করে। ভ্যালিডেশন লস পর্যবেক্ষণ করে; যদি টানা ৫টি এপোক লস না কমে তবে আর্লি স্টপিং দিয়ে ট্রেনিং শেষ করে।
    7. সেরা মডেলের ওজন (`weights.pt`), `training_history.json` এবং `training_provenance.json` ফাইলে ট্রেনিং লগ সংরক্ষণ করে।

---

### ৫.৫. `scripts/calibrate_thresholds.py` (অপটিমাল থ্রেশহোল্ড ক্যালিব্রেটর)
* **উদ্দেশ্য:** প্রতিটি মডেলের জন্য এমন একটি স্কোর কাটঅফ নির্ধারণ করা যা সত্য উত্তর গ্রহণ এবং অপ্রাসঙ্গিক প্রশ্ন প্রত্যাখ্যানে সর্বোচ্চ ভারসাম্য দেয়।
* **ফাংশন: `main()`**
  * **আর্গুমেন্ট পার্সিং:** `--models` (যেমন `a b c d`)।
  * **কার্যপ্রণালী:**
    1. `relevance_queries.csv` থেকে কেবল `validation` স্প্লিটের কুয়েরিগুলো ফিল্টার করে।
    2. প্রতিটি মডেলের জন্য `calibrate_threshold()` ফাংশন কল করে গ্রিড সুইপ চালায়।
    3. সর্বোচ্চ ব্যালেন্সড এক্যুরেসি দেওয়া থ্রেশহোল্ডটি নির্বাচন করে `artifacts/thresholds.json` ফাইলে সেভ করে।

---

### ৫.৬. `scripts/evaluate_all.py` (বেঞ্চমার্ক ইভ্যালুয়েশন ও সামারি জেনারেটর)
* **উদ্দেশ্য:** সম্পূর্ণ স্বাধীন টেস্ট সেটের ৪৮টি আনসিন কুয়েরির ওপর সব মডেলকে মূল্যায়ন করা এবং তুলনামূলক পারফরম্যান্স রিপোর্ট তৈরি করা।
* **ফাংশন: `main()`**
  * **আর্গুমেন্ট পার্সিং:** `--models` (ডিফল্ট `a b c d`)।
  * **কার্যপ্রণালী:**
    1. `evaluation_queries.csv` লোড করে এবং `validate_datasets()` দ্বারা কঠোর লিকেজ অডিট সম্পন্ন করে।
    2. প্রতিটি ক্যালিব্রেটেড মডেলকে `evaluate_model()` এর মাধ্যমে `retrieval_only` এবং `end_to_end` মোডে টেস্ট করে।
    3. ফলাফল প্রসেস করে `artifacts/evaluation/` ডিরেক্টরিতে তিনটি প্রফেশনাল রিপোর্ট তৈরি করে:
       * `comparison.csv`: মডেলগুলোর সামগ্রিক মেট্রিক্স (Hit@1, Hit@3, MRR, OOD Accuracy ইত্যাদি)।
       * `by_category.csv`: প্রশ্নের ধরণভিত্তিক (যেমন broad_topic, synonym, out_of_domain) বিস্তারিত পারফরম্যান্স।
       * `per_query.csv`: প্রতিটি কুয়েরির জন্য কোন মডেল কী উত্তর দিয়েছে তার বিস্তারিত লগ।
       * `run_manifest.json`: ইভ্যালুয়েশনের ক্রিপ্টোগ্রাফিক হ্যাশ ও রান মেটাডেটা।

---

## ৬. টেস্ট সুইট ও কোয়ালিটি অ্যাসিউরেন্স (`tests/`)

সিস্টেমের নির্ভরযোগ্যতা নিশ্চিত করতে এবং কোনো কোড পরিবর্তনের ফলে পূর্বের ফিচার যেন ভেঙে না যায় (Regression prevention), সেজন্য PyTest ফ্রেমওয়ার্কের মাধ্যমে ২৯টি টেস্ট ডিজাইন করা হয়েছে।

### ৬.১. `tests/test_core.py` (কোর লজিক, কর্পাস ও পার্সিং টেস্ট)
* **ফিক্সচারসমূহ:**
  * `courses()`: মডিউল স্কোপে ১০৮টি কোর্স লোড করে।
  * `model(courses)`: TF-IDF মডেল লোড করে টেস্টের জন্য প্রস্তুত করে।
* **টেস্ট ফাংশনসমূহ:**
  1. `test_source_complete(courses)`: সিলেবাসের ১০৮টি রেকর্ড সম্পূর্ণ কিনা, কোনো ক্রেডিট বা কন্টাক্ট আওয়ার মিসিং আছে কিনা এবং সামারি টেবিলের টেক্সট ডেসক্রিপশনে লিক হয়নি তা নিশ্চিত করে।
  2. `test_continuation_and_missing_colon()`: কোলনবিহীন হেডিং (যেমন `CSE 4245`) এবং দুই পৃষ্ঠাব্যাপী বিস্তৃত কোর্সের টেক্সট সফলভাবে জোড়া লাগার পরীক্ষা করে।
  3. `test_codes(query)`: বিভিন্ন ফরম্যাটের কোর্স কোড (যেমন `cse4121`, `CSE-4121`) সঠিকভাবে স্ট্যান্ডার্ডাইজড হয় কিনা যাচাই করে।
  4. `test_technical_terms()`: টেকনিক্যাল শব্দ যেমন `c++`, `os`, `nlp` যেন স্টপওয়ার্ড হিসেবে মুছে না যায় বা ভুল লেম্যাটাইজ না হয় তা পরীক্ষা করে।
  5. `test_parser(courses)`: কুয়েরি পার্সার যাতে ১১টি ইনটেন্ট, ইয়ার-টার্ম এবং অ্যালিয়াস (`NLP` $\to$ `Natural Language Processing`) নির্ভুলভাবে ধরতে পারে তা যাচাই করে।
  6. `test_rejections(model, query, status)`: আজেবাজে প্রশ্ন (`gibberish`), ডোমেইনের বাইরের প্রশ্ন (`out_of_scope`) এবং সিলেবাসে নেই এমন তথ্যের ক্ষেত্রে মডেল সঠিকভাবে রিজেকশন মেসেজ দিচ্ছে কিনা তা প্যারামিটারাইজড টেস্ট দিয়ে নিশ্চিত করে।
  7. `test_actual_facts_and_lab(model)`: সত্য তথ্য যেমন NLP কোর্সের ৩.০ ক্রেডিট এবং ল্যাব কোর্সের ক্ষেত্রে সেশনাল রেকর্ড সঠিকভাবে খুঁজে পাওয়া যায় কিনা তা পরীক্ষা করে।
  8. `test_year_term_all_results(model)`: শিক্ষাবর্ষভিত্তিক প্রশ্নে সংশ্লিষ্ট সেমিস্টারের সবকটি কোর্স আসে কিনা যাচাই করে।
  9. `test_year_term_lab_and_theory_queries(model)`: ৪থ বর্ষ ১ম টার্মের ল্যাব কুয়েরিতে কেবল ল্যাব কোর্স এবং থিওরি কুয়েরিতে কেবল থিওরি কোর্স ফিল্টার হয় কিনা তা যাচাই করে।
  10. `test_exact_security(model)`: "Computer Security" বা "Network Security"-র মতো সুনির্দিষ্ট কোর্সের ক্ষেত্রে সঠিক কোর্সটি টপ-১ এ আসে কিনা তা টেস্ট করে।
  11. `test_metrics_multi_gold()`: একাধিক সত্য উত্তরের ক্ষেত্রে Hit@k, MRR এবং Recall@3 গাণিতিকভাবে সঠিকভাবে কাজ করছে কিনা তা যাচাই করে।
  12. `test_split_and_negative_integrity(courses, model)`: ট্রেন, ভ্যালিডেশন ও টেস্টের মধ্যে কোনো এক্সাক্ট কুয়েরি বা গ্রুপ আইডি লিকেজ নেই এবং নেগেটিভ স্যাম্পলিংয়ে পজিটিভ কোর্স ঢুকছে না তা অডিট করে।
  13. `test_stale_artifact_refused(courses)`: সিলেবাস পরিবর্তন হওয়ার পর পুরোনো ইনডেক্স লোড করতে গেলে সিস্টেম যে কঠোরভাবে `ValueError` দেয় তা নিশ্চিত করে।

---

### ৬.২. `tests/test_neural.py` (নিউরাল আর্কিটেকচার ও ভেক্টর টেস্ট)
* **টেস্ট ফাংশনসমূহ:**
  1. `test_packed_encoder_ignores_other_sequences_padding()`:
     * **উদ্দেশ্য:** PyTorch-এর `pack_padded_sequence` ব্যবহারের ফলে ব্যাচের ছোট সিকোয়েন্সের আউটপুট যাতে বড় সিকোয়েন্সের প্যাডিং দ্বারা প্রভাবিত না হয় তা গাণিতিকভাবে নিশ্চিত করা।
     * **যাচাইকরণ:** একটি ছোট সিকোয়েন্সকে একা এনকোড করলে যে ভেক্টর আসে, বড় সিকোয়েন্সের সাথে ব্যাচ করে এনকোড করলেও যেন দশমিকের পর ৬ ঘর পর্যন্ত হুবহু একই মান আসে (`torch.allclose(alone, batched, atol=1e-6)` )।
  2. `test_vectors_keep_pretrained_words_outside_corpus(tmp_path)`:
     * **উদ্দেশ্য:** সিলেবাসে নেই কিন্তু প্রি-ট্রেইন্ড Word2Vec-এ আছে এমন শব্দ (যেমন "encryption") কুয়েরিতে আসলে যেন তার সিম্যান্টিকস অক্ষুণ্ণ থাকে এবং শূন্য ভেক্টরে পরিণত না হয় তা নিশ্চিত করা।
  3. `test_bilstm_learns_toy_pair_signal()`:
     * **উদ্দেশ্য:** BiLSTM নেটওয়ার্কের ব্যাকপ্রোপাগেশন ও গ্রেডিয়েন্ট প্রবাহ সঠিকভাবে কাজ করছে কিনা তা একটি ক্ষুদ্র টয় ডেটাসেটে ৫টি অপটিমাইজেশন স্টেপ চালিয়ে লস কমানোর মাধ্যমে যাচাই করা।

---

### ৬.৩. `tests/test_sbert.py` (Sentence-BERT আর্কিটেকচার টেস্ট)
* **ফাংশন: `test_sbert_real_index_shape_and_normalization()`**
  * **উদ্দেশ্য:** মডেল ডি-এর ডিস্কে সংরক্ষিত `document_embeddings.npy` ফাইলের মাত্রা ঠিক $108 	imes 384$ কিনা এবং প্রতিটি কোর্সের ভেক্টরের ইউক্লিডিয়ান দৈর্ঘ্য ঠিক $1.0$ ($L_2$ normalized) কিনা তা পরীক্ষা করা (`np.allclose(np.linalg.norm(matrix, axis=1), 1., atol=1e-5)` )।

---

### ৬.৪. `tests/test_app.py` (Streamlit UI ও ইন্টিগ্রেশন টেস্ট)
* **টেস্ট ফাংশনসমূহ:**
  1. `test_search_and_metadata_rerun()`:
     * হেডলেস মোডে Streamlit অ্যাপ বুট করে ডিফল্ট সার্চ বাটন ক্লিক করে দেখা হয় কোনো এক্সেপশন ছাড়া "Computer Security" ফলাফল প্রদর্শিত হয় কিনা।
     * এরপর "How many credits does NLP have?" কুয়েরি টাইপ করে "3.0 credits" রেসপন্স আসে কিনা যাচাই করা হয়।
  2. `test_same_query_switches_actual_model()`:
     * একই কুয়েরির জন্য ড্রপডাউন থেকে মডেল এ, বি, সি, এবং ডি-তে সুইচ করে প্রতিটি মডেলের পৃথক স্কোর ও অভ্যন্তরীণ মেকানিক্সের পরিবর্তন নিশ্চিত করা হয়।
  3. `test_model_d_can_be_disabled(monkeypatch)`:
     * সেন্টেন্স-বার্ট বা ভারী লাইব্রেরি কোনো মেশিনে অনুপস্থিত থাকলেও সিস্টেম ক্র্যাশ না করে গ্রেসফুলি একটি তথ্যমূলক বার্তা দিয়ে বাকি মডেলগুলোকে সচল রাখে কিনা তা টেস্ট করা হয়।

---

## ৭. ইউজার ইন্টারফেস ও ওয়েব অ্যাপ্লিকেশন (`app.py`)

ব্যবহারকারীদের জন্য তৈরি ইন্টারেক্টিভ ওয়েব অ্যাপ্লিকেশনটি Streamlit ফ্রেমওয়ার্ক দিয়ে তৈরি। এতে কোনো মডেলের রিসোর্স বারবার লোড না করার জন্য উচ্চক্ষমতাসম্পন্ন ক্যাশিং ব্যবস্থা রয়েছে।

#### ক্যাশিং ফাংশনসমূহ:

##### ১. `file_version(path)`
* **সিগনেচার:** `file_version(path: Path) -> int`
* **প্যারামিটার:** `path` — কোনো ফাইলের পাথ।
* **রিটার্ন:** ফাইলের সর্বশেষ মোডিফিকেশন টাইমস্ট্যাম্প (ন্যানোসেকেন্ডে) অথবা ০।
* **ভূমিকা:** ফাইলের ডিস্ক স্টেট ট্র্যাক করে ক্যাশ ইনভ্যালিডেশন পরিচালনা করে।

##### ২. `course_data(version)`
* **সিগনেচার:** `course_data(version: int) -> list[dict]`
* **ডেকোরেটর:** `@st.cache_data`
* **ভূমিকা:** মেমরিতে ১০৮টি কোর্সের ডেটাবেজ ক্যাশ করে রাখে। ফাইল মডিফাই না হওয়া পর্যন্ত ডিস্ক থেকে দ্বিতীয়বার পড়ে না।

##### ৩. `array_data(model_id, filename, version)`
* **সিগনেচার:** `array_data(model_id: str, filename: str, version: int) -> np.ndarray`
* **ডেকোরেটর:** `@st.cache_data`
* **ভূমিকা:** মডেলগুলোর প্রি-কম্পিউটেড NumPy ম্যাট্রিক্সগুলো (যেমন `document_embeddings.npy`) মেমরিতে ক্যাশ করে।

##### ৪. `comparison_data(version)`
* **সিগনেচার:** `comparison_data(version: int) -> pd.DataFrame`
* **ডেকোরেটর:** `@st.cache_data`
* **ভূমিকা:** ইভ্যালুয়েশন বেঞ্চমার্কের `comparison.csv` ফাইল ক্যাশ করে যাতে UI-তে তৎক্ষণাৎ চার্ট ও টেবিল রেন্ডার হতে পারে।

##### ৫. `cached_model(model_id, corpus_version, manifest_version, threshold_version, vector_version)`
* **সিগনেচার:** `cached_model(...) -> BaseRetriever`
* **ডেকোরেটর:** `@st.cache_resource`
* **ভূমিকা:** এটি অত্যন্ত গুরুত্বপূর্ণ একটি ফাংশন। ভারী নিউরাল নেটওয়ার্ক, ওয়ার্ড২ভেক বা সেন্টেন্স-বার্ট মডেলকে একবার মেমরিতে লোড করে গ্লোবাল সিঙ্গেলটন হিসেবে রেখে দেয়। এর ফলে ব্যবহারকারী প্রশ্ন টাইপ করলে মিলি-সেকেন্ডের মধ্যে উত্তর প্রদর্শিত হয়, কোনো লোডিং ল্যাগ হয় না।

#### UI লেআউট ও ইন্টারঅ্যাকশন লজিক:
* **পৃষ্ঠার কনফিগারেশন:** `st.set_page_config(page_title="KUET Academic IR", page_icon="📚", layout="wide")`
* **সাইডবার:** মডেল নির্বাচন ড্রপডাউন (`Model A, B, C, D`), মডেল আর্কিটেকচার বিবরণী, থ্রেশহোল্ড ইনফরমেশন এবং সার্বিক সিস্টেম মেট্রিক্স প্রদর্শন।
* **সার্চ ইনপুট ও বাটন:** ব্যবহারকারীর প্রশ্ন টাইপ করার টেক্সট বক্স এবং প্রি-সেট কুইক কুয়েরি বাটনসমূহ।
* **রেজাল্ট উপস্থাপন:** স্ট্যাটাস অনুযায়ী `st.success`, `st.info` বা `st.warning` কার্ডের মাধ্যমে মানুষের পাঠযোগ্য একাডেমিক উত্তর উপস্থাপন।
* **"Show Processing Details" (ভাইভা এক্সপ্যান্ডার):** পার্স করা ইনটেন্ট, এক্সট্র্যাক্ট করা ফিল্টার, টপ স্কোর, থ্রেশহোল্ড কাটঅফ এবং মডেলের অভ্যন্তরীণ মেকানিক্স (যেমন TF-IDF শব্দের ওয়েট বা BiLSTM লজিট) সরাসরি প্রদর্শন করে।
* **কোর্স প্রোফাইল ড্রয়ার:** প্রতিটি কোর্সের ক্রেডিট, কন্টাক্ট আওয়ার, পেজ রেফারেন্স এবং মূল সিলেবাস ডেসক্রিপশন সুন্দরভাবে ভিউ করার সুবিধা।
* **মডেল তুলনা ট্যাব:** টেস্ট সেটের ওপর চারটি মডেলের পারফরম্যান্সের তুলনামূলক টেবিল ও গ্রাফ।

---

## ৮. ডেটা ফাইল ও সাপোর্টিং কনফিগ ফাইলসমূহ

কোডবেসের পাশাপাশি প্রজেক্টের সঠিক পরিচালনা ও প্রমাণের জন্য বিদ্যমান নন-পাইথন ফাইলগুলোর উদ্দেশ্য ও গঠন নিচে ব্যাখ্যা করা হলো:

### ৮.১. `data/raw/CourseContentsCSE_14_08_2022.pdf`
* **উদ্দেশ্য:** এটি খুলনা প্রকৌশল ও প্রযুক্তি বিশ্ববিদ্যালয় (KUET)-এর কম্পিউটার সায়েন্স অ্যান্ড ইঞ্জিনিয়ারিং (CSE) বিভাগের অফিসিয়াল ১১২ পৃষ্ঠার সিলেবাসের মূল PDF ফাইল। এটি সম্পূর্ণ প্রজেক্টের গ্রাউন্ড ট্রুথ বা তথ্যের মূল উৎস।

### ৮.২. `data/processed/courses.json`
* **উদ্দেশ্য:** কাঁচা PDF থেকে অ্যালগরিদমের মাধ্যমে পার্স করা ১০৮টি কোর্সের মূল ডেটাবেজ।
* **প্রতিটি কোর্সের স্কিমা (JSON Schema):**
  * `record_id`: কোর্সের অনন্য সনাক্তকারী (যেমন `CSE_4121_4th_Year_1st_Term`)।
  * `course_code`: প্রমিত কোর্স কোড (`CSE 4121`)।
  * `course_title`: কোর্সের পূর্ণাঙ্গ শিরোনাম (`Natural Language Processing`)।
  * `year`: শিক্ষাবর্ষ (`4th Year`)।
  * `term`: সেমিস্টার/টার্ম (`1st Term`)।
  * `credits`: ক্রেডিট সংখ্যা ফ্লোট আকারে (`3.0`)।
  * `contact_hours`: সাপ্তাহিক ক্লাস আওয়ার (`3.0L+0.0P`)।
  * `course_type`: কোর্সের ধরন (`Theory`, `Laboratory`, অথবা `Project`)।
  * `optional_group`: ঐচ্ছিক বিভাগ (`Optional-I`) অথবা `None`।
  * `description`: সিলেবাসের বিস্তারিত টপিক ডেসক্রিপশন।
  * `search_text`: ডাবল-ওয়েটেড টাইটেল সহ রিট্রিভাল অপটিমাইজড টেক্সট।
  * `source_page`: সিলেবাসের মূল পৃষ্ঠা নম্বর (`13`)।
  * `source_conflicts`: সামারি টেবিলের সাথে কোনো বৈপরীত্য থাকলে তার তালিকা।

### ৮.৩. `data/processed/courses.csv`
* **উদ্দেশ্য:** গবেষক এবং শিক্ষকদের জন্য সহজে স্প্রেডশিট বা এক্সেলে দেখার উপযোগী ১০৮টি কোর্সের কমা-সেপারেটেড সংস্করণ।

### ৮.৪. `data/processed/corpus_statistics.json`
* **উদ্দেশ্য:** কর্পাসের সার্বিক ডেটা কোয়ালিটি ও অডিট রিপোর্ট। এতে মোট পৃষ্ঠা (১১২), ইউনিক কোর্স কোড (১০৭টি কোড, ১০৮টি অফারিং কারণ একটি কোর্স ভিন্ন ভিন্ন বছরে অফার করা হয়েছিল), থিওরি কোর্স (৬৫টি), ল্যাব কোর্স (৩৮টি), প্রজেক্ট (৫টি), এবং কোনো মিসিং ফিল্ড নেই তা সুস্পষ্টভাবে নথিভুক্ত থাকে।

### ৮.৫. `data/queries/aliases.json`
* **উদ্দেশ্য:** বহুল ব্যবহৃত টেকনিক্যাল সংক্ষিপ্ত রূপের সাথে সিলেবাসের আনুষ্ঠানিক নামের ম্যাপিং ফাইল:
  * `"NLP"`: `"Natural Language Processing"`
  * `"ML"`: `"Machine Learning"`
  * `"OS"`: `"Operating Systems"`
  * `"DBMS"`: `"Database Systems"`
  * `"CN"`: `"Computer Networks"`
  * `"DSA"`: `"Data Structures and Algorithms"`

### ৮.৬. `data/queries/relevance_queries.csv` (৭২টি কুয়েরি)
* **উদ্দেশ্য:** মডেলের ট্রেনিং এবং থ্রেশহোল্ড ক্যালিব্রেশনের জন্য ব্যবহৃত ডেটাসেট।
* **কলামসমূহ:** `query_id`, `query_text`, `query_category`, `gold_course_code`, `has_answer`, `split` (`train` অথবা `validation`), `group_id`।

### ৮.৭. `data/queries/evaluation_queries.csv` (৪৮টি কুয়েরি)
* **উদ্দেশ্য:** মডেলের টেস্ট সেটের আনসিন কুয়েরি ডেটাসেট। কোনো মডেলকে ট্রেইন বা টিউন করার সময় এই ডেটা ব্যবহার করা হয়নি; ফলে এটি মডেলগুলোর আসল নিরপেক্ষ পারফরম্যান্স যাচাই করে।

### ৮.৮. `start_app.bat` (উইন্ডোজ এক্সিকিউটেবল স্ক্রিপ্ট)
* **উদ্দেশ্য:** উইন্ডোজ সিস্টেমে ব্যবহারকারী কোনো কমান্ড মুখস্থ না রেখেই যাতে ডাবল-ক্লিক করে অ্যাপ চালাতে পারেন।
* **লজিক:** স্ক্রিপ্টটি প্রথমে চেক করে প্যারেন্ট ডিরেক্টরিতে ভার্চুয়াল এনভায়রনমেন্টের পাইথন (`..\Scripts\python.exe`) আছে কিনা, না থাকলে লোকাল `.venv\Scripts\python.exe` বা সিস্টেম পাইথন দিয়ে `streamlit run app.py` চালু করে।

### ৮.৯. `requirements.txt` ও `requirements-tested.txt`
* **`requirements.txt`:** মূল প্যাকেজের ফ্লেক্সিবল সংস্করণ স্পেসিফিকেশন (যেমন `torch>=2.6,<3`, `streamlit>=1.45,<2`, `pymupdf>=1.26,<2` ইত্যাদি)।
* **`requirements-tested.txt`:** ডেভেলপমেন্ট ও টেস্টের সময় ব্যবহৃত হুবহু পাইথন প্যাকেজের ফ্রোজেন ভার্সন তালিকা যা শতভাগ রিপ্রোডিউসিবিলিটি নিশ্চিত করে।

### ৮.১০. `README.md`, `IMPLEMENTATION_REPORT.md` ও `explain.md`
* **`README.md`:** প্রজেক্ট সেটআপ ও কুইক-স্টার্ট নির্দেশিকা।
* **`IMPLEMENTATION_REPORT.md`:** সিস্টেমের টেকনিক্যাল ডিজাইন, ম্যাট্রিক্স ডাইমেনশন ও রিট্রিভাল ট্রেড-অফের ওপর পূর্ণাঙ্গ রিপোর্ট।
* **`explain.md`:** ভাইভা ও টেকনিক্যাল ডিপ-ডাইভের জন্য ২১টি সেকশনে রচিত বিস্তৃত বাংলা রেফারেন্স গাইড।

### ৮.১১. `report/main.tex` ও `report/main.pdf`
* **উদ্দেশ্য:** বিশ্ববিদ্যালয়ের শিক্ষক ও ল্যাব মূল্যায়নের জন্য আন্তর্জাতিক একাডেমিক ফরম্যাটে রচিত সম্পূর্ণ LaTeX প্রজেক্ট রিপোর্ট এবং তার কম্পাইল করা PDF ডকুমেন্ট। এতে ভূমিকা, কর্পাস ইঞ্জিনিয়ারিং, চারটি মডেলের গাণিতিক ফর্মুলেশন, এক্সপেরিমেন্টাল রেজাল্টস এবং ভবিষ্যৎ কর্মপরিকল্পনা সন্নিবেশিত রয়েছে।

---

## ৯. সংক্ষেপণ ও উপসংহার

এই প্রজেক্টের প্রতিটি ফাইল একটি সুনির্দিষ্ট দায়িত্ব পালন করে এবং কোনো ফাইলে কোনো অপ্রয়োজনীয় বা অব্যবহৃত কোড রাখা হয়নি:
1. কাঁচা ডেটা এক্সট্র্যাক্ট করে **`src/corpus/`**।
2. টেক্সট ক্লিন ও ইনটেন্ট বোঝে **`src/preprocessing.py`** ও **`src/query_parser.py`**।
3. বিভিন্ন অ্যালগরিদমে সার্চ করে **`src/models/`**।
4. মানুষের ভাষায় উত্তর সাজায় **`src/answer_formatter.py`**।
5. কঠোর নিরপেক্ষ মূল্যায়ন নিশ্চিত করে **`src/evaluation.py`** ও **`src/datasets.py`**।
6. সম্পূর্ণ সিস্টেমকে মানুষের সামনে উপস্থাপন করে **`app.py`**।

এই ডকুমেন্টটি পাঠ করার পর প্রজেক্টের যেকোনো ফাইলের উদ্দেশ্য এবং যেকোনো ফাংশনের লাইন-বাই-লাইন কার্যপ্রণালী সম্পর্কে সম্পূর্ণ সুস্পষ্ট ও নির্ভরযোগ্য ধারণা লাভ করা সম্ভব।
